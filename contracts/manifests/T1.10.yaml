scope_id: T1.10
scope_description: >
  Mapper sub-regime threshold sensitivity sweep for P01-A B12: recompute the
  sub-regime node count at |z| in {1.0, 1.5, 2.0}, report the permutation
  count B, and apply a Benjamini-Hochberg FDR correction to per-node
  z-scores for individual-node identification.
authored: "2026-06-07"
authored_by: "Manager 9 (pre-dispatch coverage extraction)"

contracts:
  - stage1-output-schemas/mapper-threshold-sweep-output
  - stage1-output-schemas/mapper-threshold-sweep-output-json-validation

notes: >
  Contracts are Manager-authored and pending before Worker execution. The
  Worker must implement the binding tests in
  tests/trajectory_tda/test_stage1_aux_diagnostics_contracts.py and clear
  pending only after the binding tests exist and pass. If binding
  implementation is blocked, the Worker reports Partial and leaves the
  result unaccepted for paper claims.
