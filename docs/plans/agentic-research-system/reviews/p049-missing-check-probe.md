# P-049 missing-check probe marker

This intentionally inert marker is committed with GitHub's documented
`[skip ci]` directive solely to leave the permanent required checks absent on
its pull request.

It changes no runtime, research, Gate, policy, or delivery behaviour and will
be closed without merge after the missing-check admission block is recorded.
