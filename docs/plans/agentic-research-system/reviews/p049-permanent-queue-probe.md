# P-049 permanent queue-probe marker

This intentionally inert marker is the final positive test subject for the
permanent `main` merge-admission ruleset.

It changes no runtime, research, Gate, policy, or delivery behaviour. Its only
purpose is to prove that the installed ruleset runs the two required controls
on a fresh GitHub merge-group subject before merging.
