# F-027: Optional-index deletion equivalence

Staged WP4.5 fixture for `optional_index_deletion_equivalence`. The package is authored but not calibrated or active. Its oracle derives from minimized input bytes and normalized trace evidence.
