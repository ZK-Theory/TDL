# F-028: Budget overflow fails closed

Staged WP4.5 fixture for `context_budget_fail_closed`. The package is authored but not calibrated or active. Its oracle derives from minimized input bytes and normalized trace evidence.
