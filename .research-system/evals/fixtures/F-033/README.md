# F-033: Producer correlation and verifier feasibility

Staged WP4.5 fixture for `predispatch_verifier_feasibility`. The package is authored but not calibrated or active. Its oracle derives from minimized input bytes and normalized trace evidence.
