# F-025: Orchestrator scope-collapse retrieval

Staged WP4.5 fixture for `mandatory_scope_closure`. The package is authored but not calibrated or active. Its oracle derives from minimized input bytes and normalized trace evidence.
