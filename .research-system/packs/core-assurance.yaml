schema_version: '1.0.0'
pack_id: core-assurance
lanes:
  topology: {failure_consequence: blocked}
  stochastic_null: {failure_consequence: blocked}
  statistical_panel: {failure_consequence: blocked}
  representation: {failure_consequence: blocked}
  output_provenance: {failure_consequence: blocked}
  paper_claim: {failure_consequence: blocked}
