# T1.5b — H2 Check rerun at L=1000 (unblock + reconfirm)

**Agent:** tda-agent (APM Worker) · **Branch/worktree:** `run/h2-positive-control` (`.apm/worktrees/run-h2-positive-control`) · **Papers:** P01-A / P01-B (shared H2 restriction) · **Dispatched by:** Manager 10

## Governing pre-registration (READ FIRST)
- 2026-05-13 "H2 Check" **as amended 2026-06-17** → `results/trajectory_tda_integration/stage1/pre_registrations_2026-06-17.json`.
- Registered (amended) key params: **L=1000, B=50, maxdim=2, null=markov-1, seed=42**. Decision rule UNCHANGED: *reject → expand to H2; fail → restriction stands*.
- **Expected outcome:** H2 not significant (the 2026-05-03 L=1000 run gave p=0.690 ns). This is a reconfirmation under frozen-loadings / locked-env — not a discovery run.
- **🚩 If the H2 null REJECTS, STOP and escalate to the Manager before any prose** — that contradicts the recorded decision and the pre-reg.
- **Do NOT attempt L=2000** (pre-registered infeasible; it froze the host twice on 2026-06-17).

## Objective
Make the dimension-2 H2 critical path tractable and rerun the H2 Check at L=1000, producing the observed H2 PH + the B=50 Markov-1 null + the diagnostics summary, with complete provenance, without host instability.

## Code changes (minimal, opt-in, estimand-preserving)
1. **`poverty_tda/topology/multidim_ph.py::compute_rips_ph`** — add opt-in params, **defaults unchanged** so existing poverty_tda callers are unaffected:
   - `do_cocycles: bool = True` (pass-through to ripser).
   - `method: str = "ripser"`; `method="collapse"` selects a GUDHI edge-collapse path: `RipsComplex(points=X, max_edge_length=thresh)` → `create_simplex_tree(max_dimension=1)` → `collapse_edges(nb_iterations=…)` → `expansion(max_dim+1)` → `compute_persistence()` → diagrams. (cocycles are `None` on the collapse path; fine for H2.)
2. **Pre-flight feasibility gate** (new helper, e.g. `_assert_h2_feasible`): sample pairwise distances at the chosen threshold, estimate `edge_prop` and candidate-tetrahedron burden (~`C(L,4)·p^6`), and raise a clear, **catchable** `RuntimeError` above a configured budget. Wire it into the H2 path BEFORE any ripser/expansion call. Calibrate so L=1000 passes and an L=2000-equivalent burden is rejected.
3. **`trajectory_tda/scripts/run_t1_5_diagnostics.py::run_h2`** — call the hardened path with `do_cocycles=False, method="collapse"`; pass `--L 1000 --requested-L 2000 --fallback-reason "host-memory infeasibility at L=2000; see pre_registrations_2026-06-17.json"`.
   - **VERIFY (do not assume)** whether the B=50 Markov-1 null (`permutation_test_trajectories`, `max_dim=2`, in `trajectory_tda/topology/permutation_nulls.py`) routes through `compute_rips_ph` or its own PH path. Harden **whichever** maxdim=2 PH calls are on the critical path so observed and null use the **same exact method** and **both** are feasible at L=1000.
4. **Provenance** into the H2 result JSONs: `ph_method`, `do_cocycles`, `threshold_rule`+value, `edge_prop_at_thresh`, `L`, `requested_L`, `fallback_reason`, per-call wall-time, gudhi+ripser versions, seed. Extend `validate_h2_positive_control_diagnostics_payload` to require these.

## Safety / compute
- Run each PH call in a child process (multiprocessing/subprocess) with a wall-time timeout, so a pathological call is contained and the B=50 checkpoints survive. `psutil`/`win32job` are **NOT installed** → no RSS cap; the pre-flight gate is the primary prevention. (You may add `psutil` as a dep for a true kill-switch, but it is not required.)
- Per-replicate checkpoint for the B=50 null; resume-safe. ≥4 workers; honour `--max-hours`.

## Validation (binding)
- Author the binding test for the Manager contract **`h2-edge-collapse-exactness`** (`contracts/topology-invariants/h2-edge-collapse-exactness.yaml`, currently `pending: true`): in `tests/trajectory_tda/test_h2_edge_collapse_contracts.py::test_h2_edge_collapse_exactness_vs_dense`, assert collapse-H2 ≡ dense-H2 (bottleneck ≤ 1e-5, equal finite counts) at L∈{100,200}, seed 42. Then **remove `pending: true`** from the contract.
- Add a unit test that the pre-flight gate **raises** on an L=2000-equivalent burden and **passes** at L=1000.
- Run: `uv run pytest tests/trajectory_tda/test_h2_edge_collapse_contracts.py -q` (+ existing T1.5 contract tests); `uv run python .claude/hooks/contract_binding_check.py` (all contracts green, including the de-pended one); `uv run ruff check . && uv run ruff format .`.

## Deliverables (two-path rule)
- **Committed on `run/h2-positive-control`:** hardened `multidim_ph.py`, `run_t1_5_diagnostics.py`, the new binding test, the de-pended contract, and date-suffixed H2 result JSONs under `WORKTREE/results/trajectory_tda_integration/h2_check/` (observed `ph_H2_L1000_<date>.json`, null `04_nulls_wasserstein_h2_L1000_<date>.json`, `diagnostics_summary_<date>.json`).
- **PROJ_ROOT (gitignored, regenerable):** per-replicate checkpoints. Log the regeneration command in the Task Log.
- **Vault:** `[RESULT]` entry in `04-Methods/Computational-Log.md` (top-of-page) referencing the 2026-06-17 amendment and reporting the H2 p-value + decision-rule branch. If H2 rejects → escalate, no `[RESULT]` claim until Manager review.

## Acceptance
Observed H2 + B=50 Markov-1 null complete at L=1000 without host instability; decision-rule branch recorded (expected: fail-to-reject → restriction stands); `h2-edge-collapse-exactness` test green and de-pended; pre-flight gate test green; provenance complete; ruff + contract hooks pass.

## Report
Post the Task Report to the Manager bus per APM on completion (or on the escalation trigger above).
