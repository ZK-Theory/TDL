# T1.5e — H2 residual characterization (power + normalizer/embedding robustness)

**Agent:** tda-agent-2 (APM Worker) · **Branch/worktree:** `run/h2-positive-control` · **Papers:** P01-A / P01-B · **Dispatched by:** Manager 10 · **Follows:** T1.5d (escalated — robust scale-invariant residual)

## Why this run (READ FIRST)
T1.5d found that scale-normalization (÷ median pdist) **halved** the raw H2 T-ratios and **reversed** the birth/death location shift, confirming ~half the raw H2 rejection was cloud dispersion — but a **residual survives**: normalized W2 AND landscape-L2 both reject at L=200/300 (clean 3.5–5.6σ separation; B=50 floored p understates it; scale-invariance binding test passed). The pre-registered "reject" branch genuinely fired. Before expand-vs-restriction is locked, the residual must be characterized — it is modest (T~1.7–2.1), small-L, embedding-dependent, and **median-pdist normalization reversed the location sign**, so it could be robust scale-invariant structure OR a normalizer-choice artifact.

**Governing pre-registration:** `results/trajectory_tda_integration/stage1/pre_registrations_h2_characterization_2026-06-19.json`. Decision rule UNCHANGED (*reject → expand; fail → restriction stands*) applied to the **robust** residual.

## Reuse, don't rebuild
Extend `trajectory_tda/scripts/run_h2_dispersion_dualmetric.py` and the `normalize_landmarks_by_median_pdist` null-worker flag (T1.5d, `c58155f`): generalize the normalizer to a **parameter** ∈ {`median-pdist`, `max-pdist` (enclosing radius), `mean-pdist`}. Keep the de-pended `h2-dispersion-normalization-scale-invariance` contract green; extend its fixture coverage to the new normalizers (each must be scale-invariant ≤1e-6).

## Components
1. **Power (primary).** `median-pdist`-normalized dual-metric (W2 + landscape-L2) H2 at **L∈{200,300}, B=1000**, seed 42, Markov-1 — a real (non-floor) p-value. ≥4 workers + per-replicate checkpoint + resume; honour `--max-hours`. (T1.5d B=50 ≈ 26 min; budget for ~20× on the normalized cells.)
2. **Normalizer robustness (the crux).** Repeat normalized dual-metric H2 under **`max-pdist`** and **`mean-pdist`** at **B=50** (direction/existence, not a fine p), L∈{200,300}. For each normalizer report obs vs null H2 **birth/death medians** and the W2/landscape-L2 T-ratios. **The residual is robust iff obs differs from null in the SAME direction across normalizers**; a sign flip or vanishing rejection under an alternative normalizer means the median-pdist residual is a normalization-choice artifact.
3. **Stratification (descriptive).** On the normalized diagrams, report whether the surviving obs-vs-null difference concentrates in a few moderate-persistence H2 features or the near-diagonal bulk (obs vs null feature counts + W2 contribution by persistence band).
4. **Embedding-dependence (BEST-EFFORT).** Re-run `median-pdist`-normalized dual-metric H2 (B=50) on the **2026-05-02 pre-refreeze embedding** IF recoverable from git history / a cache; if not recoverable without disproportionate effort, **document the discrepancy as a representation-robustness caveat and note it in the report** (do not block on it).

## Decision logic (from the pre-reg)
- **Residual NOT robust** — vanishes at B=1000 (unlikely) OR obs-vs-null direction **flips / rejection vanishes** under `max-pdist` or `mean-pdist` → the median-pdist residual is a **normalizer artifact → restriction stands** (disclosed). Worker may report this as the resolved outcome.
- **Residual robust** — B=1000 confirms rejection (p<0.01, not floor) AND direction **consistent across ≥2 of {median,max,mean}-pdist** → genuine scale-invariant H2 difference → **STOP and escalate** to the Manager for the expand-vs-restriction interpretation/additivity call (NOT auto-expanded). Provide the stratification + embedding diagnostics to inform it.

## Validation
- Extend `tests/trajectory_tda/test_h2_dispersion_control.py` so the scale-invariance contract covers all three normalizers (rescale 0.5/2.0 → normalized H2 diagram + W2/landscape-L2 invariant ≤1e-6); keep the contract **de-pended** and green.
- `uv run python .claude/hooks/contract_binding_check.py` (all green); `ruff check`/`format` on touched files (repo-wide pre-existing debt out of scope).

## Deliverables (two-path rule)
- **Committed on `run/h2-positive-control`:** the parameterized-normalizer driver/worker change, the extended test, and date-suffixed `WORKTREE/results/trajectory_tda_integration/h2_check/h2_characterization_<date>.json` (B=1000 median + B=50 {max,mean} × {L200,L300} × {W2,landscape-L2}, with birth/death medians, stratification, and the embedding-dependence cell or its infeasibility note).
- **PROJ_ROOT (gitignored):** per-replicate checkpoints; regeneration command in the Task Log.
- **Vault:** NO `[RESULT]`/decision claim — Manager files the entry + applies the decision rule after review. Report numbers to the Manager bus (or the escalation trigger).

## Acceptance
B=1000 median-pdist power cell + B=50 {max,mean}-pdist robustness cells complete at L∈{200,300}; normalizer-direction consistency reported; stratification + embedding-dependence (or infeasibility note) included; scale-invariance test green for all normalizers; contract gate + touched-file ruff pass; result JSON committed; Task Report on the Manager bus per the decision branch.
