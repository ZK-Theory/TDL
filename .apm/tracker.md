---
title: P01-A and P01-B Reviewer-Response Revision to v2
---

# APM Tracker

## Task Tracking

**Stage 0:**

| Task | Status | Agent | Branch |
|------|--------|-------|--------|
| 0.1 | Done | reproducibility-agent | |
| 0.2 | Done | reproducibility-agent | |
| 0.3 | Paused — bus overwritten by T0.12; awaiting User canary_machine2 file. Only gates T2.18 prose, not Stage 1. | reproducibility-agent | pipe/two-machine-check |
| 0.4 | Done | tda-agent | |
| 0.5 | Done | tda-agent | |
| 0.6 | Done | tda-agent | |
| 0.7 | Done | tda-agent | |
| 0.8 | Done | tda-agent | |
| 0.9 | Done | panel-statistics-agent | |
| 0.10 | Done | panel-statistics-agent | |
| 0.11 | Done | panel-statistics-agent | |
| 0.12 | Done — merged 2026-05-22 (commit c3a0be2 pre-rewrite / 285075e post-rewrite) | reproducibility-agent | pipe/stage1-phase-split |
| 0.13 | Done — merged 2026-05-24 (commit c37682d → merge b66ca02). Three stage1 phase scripts now default --usoc-dir/--bhps-dir to absolute PROJ_ROOT paths via core.proj_root(). Unblocks T1.2b/c/d/f/g. | reproducibility-agent | pipe/phase-script-projroot-defaults |
| 0.14 | Done — **merged 2026-06-04 (PR [#33](https://github.com/stephendor/TDL/pull/33) → `efc9fb5`)**. Verified vs artifacts (7/7 fixture tests, warn-default exit 0 across 50 contracts, enforce blocks, dual-tree byte-identical). CodeRabbit batch (4) addressed `b72b016` (dict NameError, gate-1b regex/radius, README --no-verify, fixture assert); register corrected to 112. Merge-conflict on `schema-contract-design` skill (vs PR #32) resolved `8d1fd34` (combined lessons + gate rules, byte-identical). | reproducibility-agent | |
| 0.15 | Done — **merged 2026-06-04 (PR [#35](https://github.com/stephendor/TDL/pull/35) → `45af181`)**. Verified vs artifacts: bhps JSON byte-identical to main (inferred backfill reverted), `legacy_exempt` primitive + self-test, `icc-cluster-bootstrap` 6/6 grounded, TDA residual 0. CodeRabbit batch (`077f243`): numeric-sanity on comparison-arm stats (finite + p∈[0,1] + ratio≥0) + docstrings on 7 diff-touched helpers (coverage 69.6%→100%). Worktree swept. | tda-agent | |
| 0.16 | Done — **merged 2026-06-04 (PR [#34](https://github.com/stephendor/TDL/pull/34) → `978f76a`)**. Residual 63→0, 23 panel tests pass, E6 active; 9 pre-existing fitting JSONs grandfathered (staged-only; `--all-jsons` backlog). CodeRabbit batch (3) addressed `dece050` (docstrings on `assert_type`/`validate_sample_provenance_ledger`, `@pytest.mark.integration` on the file-dependent test, dropped unused `math` imports). Worktree git-deregistered (folder locked → manual delete). **T0.17 refinement:** narrow E6 glob from all-panel-JSON to fitting outputs. | panel-statistics-agent | |
| 0.17 | Done — **merged 2026-06-05 (PR [#36](https://github.com/stephendor/TDL/pull/36) → `0beab81`)**. Default=enforce (blocks defective fixture; 52 contracts pass), E6 path-narrowed + legacy_exempt, Contract-Quality Gate in all 4 guides. CodeRabbit (`860eaf9`): POSIX-normalized `file_dispatch` matching (PurePosixPath, Windows-safe) + escaped `ε*` md emphasis. **CONVENTIONS single-source resolved (User decision):** repo-root `CONVENTIONS.md` is canonical (full 445-line content + enforcement rule); CLAUDE.md updated; **vault file hardlinked to it** (one inode — `fsutil` confirms both paths; symlink fell back to hardlink as Developer Mode is off). Worktree swept. | reproducibility-agent | |

**Stage 2:**

| Task | Status | Agent | Branch |
|------|--------|-------|--------|
| 2.1 | Done — merged 2026-05-22 (commit 0a91d94) | academic-writing-agent | paper/p01a-v2-evidenced |
| 2.2 | Done — merged 2026-05-22 (commit 0a91d94) | academic-writing-agent | paper/p01a-v2-evidenced |
| 2.3 | Done — merged 2026-05-24 (commit 4cdab52 → merge 46d591e). New methods-embedding-dimensionality.md section (§3.2 addendum). Important Finding caught by Worker: T1.4 longer-variant prose claim "both landmark d-estimates below D/2" was numerically wrong — USoc landmark d_TwoNN=10.06 sits *at* D/2=10. Rewritten accurately; OVER-DIMENSIONAL verdict preserved via broader D/d framing. User per-section review approved both gating items. | academic-writing-agent | paper/p01a-v2-section3-intrinsic-d |
| 2.4 | Done — reviewed and merged 2026-06-06 (commit 806363b). Drafted §4.5 escape-regression/FOO prose, §S8 FOO transparency, and 43-row claim trace; full-sample FOO sensitivity framed as fragile disclosure rather than a strong variance-component claim. User per-section approval remains pending. | academic-writing-agent | |

**Stage 1:**

| Task | Status | Agent | Branch |
|------|--------|-------|--------|
| 1.1 | Done — 6 pre-reg vault entries filed | tda-agent | run/core-tda-battery (artefacts cherry-picked to main) |
| 1.2 | Done (PROVISIONAL) — batch merged 2026-05-25 (commit bf23f4c). 7/8 sub-tasks Done, 1.2g Partial. All p-values PROVISIONAL pending T1.36 frozen-loadings fix + T1.37 rerun. | tda-agent | |
| 1.2a | Done (PROVISIONAL) — USoc headline 2026-05-24 (commit cb5eb3e). H₀ W₂ p=0.0020/Land L² p=0.0818 (metric divergence); H₁ W₂ p=0.0020/Land L² p=0.0160 (both reject). | tda-agent | |
| 1.2b | Done (PROVISIONAL) — BHPS headline @ L=5000, B=1000 (commit 75ea678). H₀ W₂ p=0.0020/T=5.05/d=+12.41 strongly rejects (stronger than USoc); H₁ W₂ p=0.9900/d=−2.35 does NOT reject (asymmetry confirmed). Wall ~1h 47min. | tda-agent | |
| 1.2c | Done (PROVISIONAL) — USoc LM sensitivity L=2500 (commit 37bff84). H₀ W₂ rises off floor p=0.0100; H₀ landscape L² p=0.32 fails. L=2500 already fails the Pre-reg #1 amendment CONFIRMATORY criterion. Wall ~73 min. | tda-agent | |
| 1.2d | Done (PROVISIONAL) — USoc LM sensitivity L=8000 (commit e354b9a). CAUTIONARY verdict: H₀ W₂ at floor T=4.45; H₁ W₂ p=0.0100 small non-monotone wiggle off floor at L=8000 vs L=5000. §3 prose locked: "Confirmed for L ≥ 5000; not a global landmark-robustness claim." Wall ~5h 57min. | tda-agent | |
| 1.2e | Done (PROVISIONAL) — landscape vectorisation sensitivity USoc+BHPS, 9 cells each from cache (commit ebc381e). Headline params (k_max=5, n_points=200) sit mid-sweep (not p-hacked); n_points invariant within k_max bin; k_max strengthens USoc rejection but weakens BHPS H₁ (asymmetry grows with k_max). Wall ~16 min. | tda-agent | |
| 1.2f | Done (PROVISIONAL) — BHPS length-matched truncate (commit d0d08ab). H₀ W₂ p=0.0020 floor but H₀ landscape fails (p=0.54); H₁ W₂ p=0.9820/d_perm=−2.32 (observed PD CLOSER to nulls than nulls to each other). **BHPS H₁ rejection does NOT survive truncation to USoc-comparable horizon.** Required new pipeline `915f1cf` (`run_bhps_length_matched.py` + `_battery_core.run_headline_from_embeddings` refactor; on this `run/` branch per User direction). Wall ~2h 21min. | tda-agent | |
| 1.2g | **DONE — resolved via T1.7 dedup amendment 2026-05-30 (tracker row was stale, frozen at the 2026-05-25 kill).** The first13 frozen run completed at feasible compute under the 2026-05-30 dedup amendment (`bhps_length_matched_first13_frozen_2026-05-30.json` on main; first13 H1 W2 p=0.001), feeding the 2026-05-31 Outcome A lock. — Prior (stale) state: Partial — first13 strategy killed at perm 90/1000 (~9% complete after ~4h 52min) by User decision 2026-05-25 ~20:55. Walltime extrapolation: 2–4 more days at locked parameters (n=5363 → L=5000 = 93% landmark fraction, ~22× per-perm wall vs T1.2f). No JSON output; no commit. Future re-run requires Pre-reg #5 amendment for asymmetric L (e.g. L_f=2500 or 3000 to give ~60% landmark fraction parity); expected ~1.5h. NOT scheduled. | tda-agent | |
| 1.2h | Done — Pre-reg #5 redo outcome lock: **B\|C ambiguous** (Outcome A excluded by T1.2f; T1.2g undetermined). §4 prose direction locked verbatim in 2026-05-25 [DECISION] entry at top of Computational-Log.md: BHPS-USoc topological difference at least partially attributable to differing trajectory horizons; single-direction era-bound claim cannot be promoted. Academic-writing-agent §4 brief ready. | tda-agent | |
| 1.3 | Done — Outcome A locked (USoc 5/7, BHPS 8/8 regimes BH-significant) | tda-agent | run/core-tda-battery (artefacts cherry-picked to main) |
| 1.4 | Done — merged 2026-05-24 (commit 411336d → merge 1776720). OVER-DIMENSIONAL verdict for both datasets (USoc TwoNN 3.59/MLE 5.80; BHPS TwoNN 3.77/MLE 6.30). No T1.5 D-sweep needed. Prose paragraph ready for academic-writing-agent §3 dispatch. | tda-agent | run/intrinsic-dimensionality-estimate |
| 1.5 | **MERGED PR #50 → `main` 2026-06-20 (merge `fdf53dd`); CodeRabbit nitpicks addressed (`3e3cb97`: repo-relative fixture paths + np.mean tidy; docstrings already landed in T1.5f) + Codacy 13 security = benign Bandit subprocess FP class (no shell/eval/exec/pickle; adjudicated); worktree `run-h2-positive-control` **SWEPT 2026-06-20** (Manager 11, User-triggered) — before removal, recovered the uncommitted doubled-n W2 robustness deliverable (`04_nulls_wasserstein_w2_L5000_doublen_2026-06-16.json`, `stable_vs_t1_2: True`, MC-stable vs T1.2) → paths scrubbed repo-relative + committed to `main` as `cb951eb` + vault [RESULT]; merged local branch `run/h2-positive-control` deleted (was `3e3cb97`).** Prior: T1.5f DONE 2026-06-20 (Manager 11) — H2 landscape-grid bug fixed → restriction-stands CONFIRMED; was pushed `c2fdde2` (no real conflict — `CONFLICTING` was a stale post-push flag; origin/main fully merged at `f892bac`).** All six old-grid-vs-corrected-grid cells preserve their verdict (median/mean reject, max-pdist collapse; corrected T marginally lower, p unchanged) → `restriction_stands_confirmed` (`h2_landscape_grid_correction_complete_2026-06-20.json`). Confirmation reused cached null diagrams + regenerated only 2 observed H2 PH (L=200/300, frozen embedding, **<3e-6 drift** vs committed summary — retires the old ~6% drift worry); no null PH recomputed, B unchanged. Conflict resolved (union merge w/ T1.8 α-sweep `f892bac`); 17 CodeRabbit hygiene fixes + 2 declines (`5047709`); landscape grid now built from both diagrams' support + correct linspace spacing + symmetry binding test; 33 tests + 77-contract gate green. Cheap-path lesson: confirmatory re-runs reuse cached upstream artifacts (caught the 14h B=1000 over-scope; obs-only regen was the true minimal compute). Original dispatch: **PR #50 follow-up (T1.5f) dispatched 2026-06-20 (Manager 11):** landscape-L2 grid order-dependence (CodeRabbit, behind the locked T1.5d/e decision) → User chose fix-code + B=1000 confirmatory re-run (apply existing T1.5e rule; escalate only if residual flips robust-across-all-normalizers); bundled with conflict resolution (vs T1.8 α-sweep on main, union merge of `permutation_nulls.py`/`_battery_core.py`/`run_stage1_aux_diagnostics.py`), 17 CodeRabbit hygiene fixes, 2 declines (edge-collapse contract stays pending; confounded-run p=0 JSON frozen), Codacy subprocess FPs. Prompt `.apm/bus/tda-agent-2/task.md`, log `task-01-05f.log.md`. Doubled-n/positive-control kept OUT of this PR (separate T1.5 sub-components to adjudicate). Prior: **Active — follow-up dispatched 2026-06-11** after Partial report: first doubled-n attempt (all-at-end write) exited opaquely after hours; smoke showed ~146s/comparison; Worker correctly stopped + surfaced. **Manager benchmark on the real cache:** exact canonical W₂ = 5.0s (H0) / 7.9–9.6s (H1) per pair → NOT the bottleneck (smoke gap ≈ contention/runner overhead); `gudhi.hera` agrees to 7 s.f. but is ~3× SLOWER → rejected. **Canonical solver retained; no methodology change, no pre-reg amendment.** Follow-up: profile gap → ≥4-worker per-pair joblib (single reusable executor, checkpoint_every 50) → production doubled-n (~2.5h @ 4 workers; per-pair W₂ arrays retained in committed JSON + contract extension) → H₂ (L=2000 maxdim=2 B=50; L=1000 >24h fallback rule stands) → positive control → summary. Original scope/params unchanged: (i) H₂; (ii) positive control (~27,280 Markov-1 sim, frozen loadings, calib [0.3,0.7]); (iii) doubled-n W₂ (n_perms=200, n_nullnull=2000, L=5000, seed=42). H2-Check pre-reg (2026-05-13); contracts `h2-positive-control-diagnostics-output{,-json-validation}` (`c5633a1`). **H2 sub-task BLOCKED→unblocked 2026-06-17:** L=2000 exact VR H2 froze host ×2 (~1.2e11 tetrahedra @ 75th-pct; reproduces 2026-05-02 RAM failure). Manager 10 filed L=2000→L=1000 pre-reg amendment (`pre_registrations_2026-06-17.json`, commit `00089f3`; B=50/maxdim=2/markov-1/seed=42/decision-rule unchanged; reverts to the 2026-05-03 decision-grounding L where H2 p=0.690 ns) + `pending` contract `h2-edge-collapse-exactness` (collapse≡dense H2, bottleneck≤1e-5; validated ~9.5e-7 @ L∈{200,300,500}) + Worker prompt `.apm/tasks/T1.5b-h2-rerun.md` (do_cocycles=False + GUDHI edge-collapse + pre-flight feasibility gate; no L=2000 attempt). **ESCALATED 2026-06-18 → HELD (Manager 10 review):** tda-agent-2 ran L=1000 (dense ripser, not collapse; 114s obs / ~80s per null), H2 **REJECTS** p=0.0 T=5.16 — contradicts 2026-05-02 baseline (p=0.690). Worker correctly stopped + withheld all prose. **Verdict: rejection NOT trusted; decision rule NOT applied; restriction NOT lifted; no [RESULT]/[DECISION]; not merged.** Three artifact signatures: (1) embedding re-frozen 2026-05-28 *after* the baseline → old H2 coords ~60-80 vs new ~9-18 (~4×); amendment premise "L=1000 reproduces 2026-05-03 p=0.690" is **void** (different estimand); (2) obs cloud 75th-pct thresh **22.26** vs 50 null clouds **~15.5** (44% scale gap → null not scale-matched to observed; per-cloud-thresholded W₂ confounds scale w/ topology); (3) null-null W₂ collapsed to ~2-4 uniformly across H0/H1/H2 + **all three dims flip to strong reject together** (uniform-shift signature, not a dim-specific void discovery). H2 Check **UNRESOLVED** pending a null-construction + obs/null-comparability audit on the *current* frozen embedding. `h2-edge-collapse-exactness` contract moot (collapse path unused), stays pending. **ROOT-CAUSED 2026-06-18 (Manager null-path read):** NOT a bug — per-cloud adaptive 75th-pct threshold puts obs (22.26) vs nulls (~15.5) on different filtration scales; global-Markov-1 surrogates genuinely less dispersed → W₂ confounds scale/dispersion w/ void topology (monotone H0>H1>H2 T-ratio = scale-offset signature); flip vs 2026-05-02 further confounded by 2026-05-28 re-freeze (~4× coord scale). **REISSUED T1.5c (matched-scale pinned-threshold fix)** = `_battery_core.probe_pinned_thresh` rule (τ=enclosing radius of obs landmarks, applied to obs+all nulls; same probe that validated P01-A H1; dedup=False so vertex counts already symmetric — threshold-only). Amendment `pre_registrations_2026-06-18.json` + vault [DECISION] + prompt `.apm/tasks/T1.5c-h2-pinned-thresh.md`. **Phase A (agent 2): small-L L∈{200,300} pinned-vs-auto diagnostic** (enclosing-radius pin H2-infeasible >few-hundred L → matched-scale H2 only feasible small-L; itself a finding). Decision rule UNCHANGED, applied to the PINNED p; expected H2 ratio collapses→~1 = restriction stands. Phase B (largest-feasible-L pinned decision run) Manager-gated on Phase A. **PHASE A DONE 2026-06-18 (tda-agent-2, Partial→escalated; commits `5b226f5`+`c5c4f10`, `h2_pinned_vs_auto_smallL_2026-06-18.json`):** pinned H2 did NOT collapse — pinned ≈ auto to 4 d.p. (L=200 H2 T=3.3239 both; L=300 H2 T=3.8424 both; τ=52.74 verified obs+all nulls). **Manager threshold-artifact hypothesis REFUTED.** New finding: rejection is robust but rests on LOW-persistence near-diagonal H2 noise (obs H2 max-pers 1.61@L200 / 1.71@L300, mean ~0.4, 142-245 features ≈ n_points; cf H1 max ~4, H0 ~19) AND the H2 Check used **W2 ALONE** — violates the mandatory landscape-L2 dual-metric rule. **HELD: "reject→expand" NOT licensed off a W2-only near-diagonal rejection (overridden by dual-metric mandate); "restriction stands" not cleanly established either.** Flip vs 2026-05-02 ns is the 2026-05-28 re-freeze (current canonical embedding genuinely carries this near-diagonal H2 difference). Next (NOT another threshold probe): dual-metric landscape-L2 + persistence-floor/stratified test → does any ABOVE-NOISE H2 signal survive. Decisive either way. Worker correctly escalated; diagnostic sound, validated on touched slice (7 tests, contract gate pass; Windows joblib/loky nested-timeout bug fixed). Untracked escalation evidence (`ph_H2_L1000_2026-06-17.json`, `04_nulls_..._L1000_2026-06-17.json`) to commit before any worktree sweep. **MANAGER INLINE SCOPING 2026-06-18 (User-approved; persistence-floor + location analysis on committed diagrams + checkpoints):** (i) flooring does NOT collapse T (stays ~2.5-3.6 to floor~1.0; not pure near-diagonal noise); (ii) **decisive — obs H2 features are LOCATION-SHIFTED ~35% to larger birth/death than nulls** (L200 obs birth med 15.0 vs null 11.2; L300 13.6 vs 10.2; similar persistence spans), i.e. a **cloud-DISPERSION shift, NOT distinct void structure** — the SAME dispersion driving H0(T≈15)/H1(T≈5-7); monotone H0>H1>H2 = its fingerprint. Location check used EXACT pipeline data (committed top-20 obs + checkpoint nulls); null-null reproduced exactly (1.865); obs full-diagram regen was ~6% off (main-vs-worktree maxmin, indicative only). **Conclusion: W2 "rejection" is dispersion re-expressed in H2, NOT additive beyond H0/H1 → "expand to H2" NOT warranted; lean RESTRICTION STANDS.** Mechanical pinned-W2 "reject→expand" OVERRIDDEN (W2-alone, dispersion-confounded; dual-metric mandate). To LOCK: narrow confirmatory follow-up — scale-normalized H2 W2 (rejection should vanish) + mandatory landscape-L2 complement. **T1.5d DISPATCH-READY 2026-06-18 (commit `a50c916`):** amendment `pre_registrations_h2_dispersion_2026-06-18.json` + prompt `.apm/tasks/T1.5d-h2-dispersion-confirm.md` + vault [DECISION]. Scale-normalize each cloud by median-pdist, recompute H2 vs markov-1 under W2 AND landscape-L2 (k_max=5/n_points=200), L∈{200,300}/B=50/seed=42; raw dual-metric as confound exhibit; scale-invariance binding test required. Decision: BOTH normalized metrics fail-to-reject → restriction stands (expected); EITHER rejects → escalate. tda-agent-2 also to commit the untracked 2026-06-17 escalation JSONs. **T1.5d DONE 2026-06-19 (tda-agent-2, Partial→escalated; commits `0cea6c0`+`c58155f`+`de7b110`, `h2_dispersion_dualmetric_2026-06-19.json`):** normalization HALVED raw T-ratios (W2 3.32→1.67@L200, 3.84→2.09@L300; landscape 3.78→1.87, 4.41→1.86) and REVERSED the location shift (raw obs births>null; normalized obs births<null 0.654 vs 0.694) — confirms ~half the raw effect was dispersion. BUT a **robust scale-invariant residual SURVIVES**: normalized W2 AND landscape-L2 both reject at both L (p=1/51 floor; clean **3.5-5.6σ** obs-null vs null-null separation). Scale-invariance binding test PASSED (≤1e-6) → not a normalization bug; `h2-dispersion-normalization-scale-invariance` contract de-pended, 12 tests + gate(68) pass; escalation-evidence JSONs committed. **Manager: dispersion diagnosis ~half right; a genuine MODEST scale-invariant H2 difference exists beyond dispersion (obs voids at ~6% smaller relative scale than markov-1) → pre-registered "reject" branch genuinely fired.** NOT auto-expanded: modest magnitude + small-L + embedding-dependent (absent on 2026-05-02 pre-refreeze) + interpretation/additivity-beyond-H0/H1 unestablished. **USER FORK:** characterize-for-finding (B=1000 + normalizer/embedding robustness + paper-claim trace) vs restriction-stands-with-honest-disclosure (H2 secondary) vs expand. **User chose CHARACTERIZE 2026-06-19. T1.5e DISPATCH-READY (commit `880f616`):** amendment `pre_registrations_h2_characterization_2026-06-19.json` + prompt `.apm/tasks/T1.5e-h2-characterization.md` + vault [DECISION]. (1) median-pdist-normalized dual-metric @ **B=1000** (real p, L∈{200,300}); (2) **normalizer robustness** under max-pdist + mean-pdist @ B=50 — residual robust iff obs-vs-null direction consistent across normalizers (CRUX: median-pdist reversed the location sign); (3) persistence-stratified description; (4) best-effort 2026-05-02-embedding re-run. **Decision:** not-robust (flips/vanishes across normalizers) → restriction stands (disclosed); robust (B=1000 p<0.01 + consistent direction) → escalate for expand-vs-restriction interpretation/additivity call (NO auto-expand). Reuses `run_h2_dispersion_dualmetric.py` + normalize flag (generalize to normalizer param); scale-invariance contract extended to all 3 normalizers. **T1.5e DONE 2026-06-19 (tda-agent-2, Partial→escalated robust-branch; commits `5bf71f7`/`58a6959`/`422d6c2`, `h2_characterization_2026-06-19.json`):** median-pdist B=1000 REJECTS real-p (W2 p=.0020/.0010, landscape p=.0010/.0030 @L200/300; T~1.75-2.1); mean-pdist B=50 rejects same direction (T~1.7-2.1); **max-pdist (diameter) COLLAPSES to T≈1.1, p≈.3-.47, NO reject.** Direction (obs voids smaller relative scale) sign-stable across all 3 (sign_flip=0) but **magnitude/significance NOT normalizer-invariant** — strong under central-tendency normalizers, ~null under diameter. Stratification: moderate-persistence (median/mean). Embedding-dependence NOT testable (2026-05-02 frozen model unserialized; worker correctly refused to refit PCA per null = old unfrozen bug). 11 tests + gate(68) pass. **Manager read: residual is real but FRAGILE — a distance-distribution-shape effect (central-tendency vs tail) manifesting in H2, normalizer-magnitude-dependent, modest, small-L, representation-uncheckable. Recommend RESTRICTION STANDS + honest disclosure (below bar for a dedicated H2 section in flagship JRSS papers).** Pre-reg routes the expand-vs-restriction/additivity call to User. **H2 CHECK RESOLVED 2026-06-19 — User decision: RESTRICTION STANDS + disclosure.** Terminal [DECISION]/[NEGATIVE] locked in Computational-Log; methodological insight crystallised in `02-Notes/Permanent/Markov-null-persistence-rejection-can-be-a-dispersion-artifact.md`. Pre-registered rule fires fail→restriction-stands: no dedicated higher-homology section for P01-A/P01-B; H2 disclosed as a limitation (mostly dispersion = H0/H1 signal + fragile normalizer-dependent residual). **H2 sub-task CLOSED.** Branch `run/h2-positive-control` carries all closed-out T1.5b-e work (+ original doubled-n/positive-control) → **ready for Manager PR merge to `main`** (then worktree sweep once CodeRabbit concludes). | tda-agent-2 | swept 2026-06-20 (was run/h2-positive-control; doubled-n recovered → `cb951eb`) |
| 1.6 | **ESCALATED ×3 2026-06-21 → Option A authorized (serial + checkpoint, ~30–35 h); attempt #4 on `tda-agent` bus.** **Attempt #3 (loky) PROVED the exact L=5000 design cannot meet 12 h on this box under ANY config: W2 on ~5000-pt H0 diagrams is memory-bandwidth-bound and does NOT parallelise** (worker scaling probe: per-distance flat 1→6 workers at ~15 s; loky slower than serial; in-process serial floor 9.88 s/distance → ~28 h calibration, ~33 h with variance; memory fine 8.7/14.7 GB). Hardened runner committed `75fba39` (loky default, static `--workers`, honest benchmark timing >workers + ctypes RSS probes, 5/5 tests). **No parallelism lever exists (3rd Manager parallelism miss — now empirically closed).** **User decision: Option A — serial in-process (9.88 s/distance) + per-trial checkpoint, wall-time budget raised, no methodology change / no amendment / no subsampling** (subsampling rejected: a credibility check must calibrate the actual L=5000 test — an L<5000 calibration certifies the wrong object). No second machine available (T0.3 machine2 not provisioned). Q2 (parallel with T1.28) rejected: T1.28 is the same bandwidth-bound L=5000 W2 class → contends, no time saved; parallel *paper writing* is the real concurrency lever. Attempt #4 prompt = add serial mode + per-trial checkpoint + lift guardrail, reuse cached null bank, run detached ~30–35 h → verdict. — Attempt #2 (B=100/8w/threading): OOM at trial 0; threading gave zero parallelism. **Attempt #2 (B=100/8w/threading) FAILED — OOM at trial 0, no verdict (Partial accepted).** Two Manager errors in the attempt-#2 config, both confirmed by the worker's data: (i) `--backend threading` gave ZERO parallelism (gudhi exact-W2 holds the GIL) — clean serial measurement **9.88 s/distance** (100 null-pair distances in 987.8 s) → realistic full design ≈ **27.7 h serial**, not the probe's 5.86 h; (ii) the `--benchmark-only` probe was **4.7× optimistic** because it timed 4 distances against 8 workers (`elapsed/count` understates serial cost when batch < pool). 8-way threading OOM'd (shared heap: ~200 MB W2 cost matrix/pair on ~4,987-pt H0 diagrams ×8 + solver internals). Worker committed run-independent provenance fix `a380b8a` (`discreteness_note` B=200→actual `bank_b`); null-pair bank cached/checkpointed (0 trials). **Diagnosis (evidence-backed):** T1.6 runner is the ONLY battery defaulting to threading; every other (`run_stage1_battery`, `_battery_core`, `build_pvalue_denominator_cleanup` — computes these exact W2 distances) uses joblib default **loky** and parallelises. **Adjudication (User 2026-06-21): attempt #3 = `--backend loky` (the real fix — per-process heaps end the shared-heap OOM) + static memory-safe `n_jobs` cap (default 6) + corrected benchmark (times >worker-count distances + logs per-process RSS as the empirical memory check); launch only if <12h AND memory fits. Even 4 loky workers ≈ 8 h; no methodology change, no amendment.** **Worker inferred-vs-observed catch (resolved 2026-06-21):** the attempt-#3 prompt first prescribed a `psutil` dynamic preflight citing `run_t1_5_diagnostics.py`. Verified: that file IS on `main` with a real preflight but it is **`ctypes`/`GlobalMemoryStatusEx`, not psutil**, and it is **not in the T1.6 worktree** (branched pre-PR#50); `psutil` is not a dependency. Resolution: **do not add psutil**; loky + static cap + benchmark-RSS suffices (matches the actual static-cap precedent `run_landmark_sensitivity.py:96`); the ctypes helper is optional. Prompt corrected on the bus. Cost-model gate strengthened (`task-assignment.md` §2.2: benchmark batch>workers, cost memory, verify backend parallelism). Secondary flags: ruff drift (`uv` 0.14.9 vs pinned hook v0.8.4) + `0d4dd50` landed format-dirty (hook apparently skipped) — to follow up. — **Attempt #1 (B=200/4w):** projected 49,570 s = 13.77 h @ 4w (>12h guardrail), STOPPED + escalated; correct. Runner + contract bindings `0d4dd50` (gate 75/75; 4 pytest). Root cause: observed-to-bank `n_trials=100 × bank_B` exact-W2 evals at L=5000; dispatch costed avoided PH not retained distance evals. Runner + active contract bindings committed `0d4dd50` (`pending` cleared on both YAMLs; gate 75/75; 4 pytest green). **Root cause:** dominant cost is the **observed-to-bank** term `n_trials=100 × bank_B=200 = 20,000` exact-W2 evals at L=5000 (+200 null-null); per-eval 2.4–7.7 s is in-band for gudhi-exact (verified `vectorisation.py:226` = `gudhi.wasserstein order=2 internal_p=2`; the greedy fallback is an approximation, unused → **no free solver lever**). The dispatch costed the *avoided* fresh PH ("near-free via cache reuse") but not the *retained* 20,200 distance evals. **Adjudication (User 2026-06-21): drop `calibration_null_bank_B` 200→100 (locked floor; validator requires ≥100) + raise workers 4→8 (box has 20 logical cores) → projected ≈3.4 h; solver already optimal; L=5000 stays (frozen, validator-locked).** Follow-up Task Prompt on `tda-agent` bus (same `log_path`, B=100/workers=8/benchmark-only confirm-then-run); awaiting User `/apm-4-check-tasks`. Process gap logged → **cost-model gate added to `task-assignment.md` §2.2**. — Prior dispatch context: Full Task Prompt on `tda-agent` bus from the 2026-06-13 pre-reg: calibration double-null KS-uniformity (100 trials, frozen Markov-1, disjoint-split cache reuse preferred — near-free, no fresh PH) + BHPS-vs-USoc null-null CV (variance_flag = bhps_cv < 0.5·usoc_cv) → verdict {credible/conditionally_credible/suspect}; suspect → STOP+escalate. Manager authored both contract YAMLs on the branch at dispatch (commit `d95ee9e`, both pending; gate passed 75/75) per the Manager-authors-contracts convention; Worker writes the binding test + clears pending. Markov-null dispersion lesson (permanent note) applied to Diagnostic 2. T1.37 caches verified present (BHPS/USoc `..._frozen_B1000_L5000_seed42_2026-05-28.npz`). Worker files [RESULT] only; §4.2.3 [DECISION] is Manager's post-review. Prior: **Ready 2026-06-16 (tda-agent free after T1.8; scope User-approved 2026-06-13); hold/stagger after T1.5+T1.9b per the T1.8 contention lesson.** Now "BHPS Markov-1 credibility under valid nulls": calibration double-null uniformity (KS vs Uniform) + BHPS-vs-USoc null-null CV at L=5000, frozen loadings; geometry KS dropped. Original label-shuffle premise void per 2026-05-25 [NEGATIVE]. Locks P01-B §4.2.3. **Pre-reg AMENDMENT filed 2026-06-13** (vault Computational-Log + `results/trajectory_tda_bhps/diagnostics/pre_registrations_2026-06-13.json`, amends 2026-05-13 H4); contract specs ready in the pre-reg (`bhps-markov1-credibility-output{,-json-validation}` — author YAMLs on the branch at dispatch). **Dispatch-ready.** Dep T1.37 (done). | tda-agent | run/bhps-markov1-credibility |
| 1.7 | Done (scope-align 2026-06-07) — satisfied by the Pre-reg #5 redo / dedup-amendment work (**PR #28 merged 2026-06-01**, branch `run/length-matched-dedup-rerun`). Both length-matching strategies (truncate + first13) reject H₁ W₂ at α=0.05 under frozen loadings + external-indexing dedup (truncate p=0.001, first13 p=0.001); **Outcome A locked 2026-05-31** → §6.2 prose: BHPS-vs-USoc H₁ signal is NOT a length-of-observation artefact. Closure artifact `dedup_amendment_comparison_2026-05-31.json` + frozen result JSONs on main; [DECISION] 2026-05-31 + CONVENTIONS dedup rule. Leftover worktree `run-length-matched-dedup-rerun` is a sweep candidate. | tda-agent | |
| 1.8 | **Done — reviewed 2026-06-16 (Manager 10); RA 5-lane PASS vs `markov2_alpha_sweep_summary_2026-06-16.json`; in PR [#47](https://github.com/stephendor/TDL/pull/47).** Pre-reg decision rule satisfied: all four dataset×dim W₂ rejection directions stable across α∈{0,0.5,1,5} (USoc H0/H1 reject p=9.99e-4; BHPS H0 reject; BHPS H1 fail-to-reject p=0.857–0.997, d_perm≈−1.0…−2.8) → `conclusion_stable=true`, `canonical_alpha=1` (commit `f190dad`). BHPS H1 non-rejection at every α **corroborates** the §6.2 size/landmark-fraction artefact. Null spot-checks pass ((r+1)/(B+1); B=1000, seed=42, markov_order=2, frozen loadings). W₂-only by pre-reg design (decision rule is W₂-direction stability; dual-metric governs the headline, not this robustness sweep). Important finding operational only (cell×perm parallelism contention; `--n-jobs 1 --permutation-jobs 4` resolved). **Provenance gap fixed:** the CONVENTIONS α=1 lock had been written only through the vault hardlink (never in git) → added to repo-canonical `CONVENTIONS.md` on-branch + ignored the per-cell checkpoint/status JSONs (commit `a1d9248`). **CodeRabbit batch `69bb371`** (9 findings, all valid, none skipped; paths→repo-relative byte-identical, checkpoint load-time guard, fail-fast L/seed/B, alpha≥0, docstrings/footer/purity-test) — confirmed none altered the run numbers. **PR #47 MERGED 2026-06-16 (merge `df1e737`); worktree + branch (local+remote) swept; vault↔repo CONVENTIONS hardlink re-created (fsutil-confirmed).** | tda-agent | swept (was run/markov2-alpha-sweep) |
| 1.9 | **DONE — superseded by T1.9b (merged PR #51, 2026-06-20); tracker row was stale.** T1.9b re-analysed with inferential grounding → outcome `newcomers_robust`; §6/§3.4.2 [DECISION] locked at top of Computational-Log, superseding this divergent/inconclusive single-draw point estimate. — Prior (stale) state: Reported Success but DIVERGENT/INCONCLUSIVE — §6/§3.4.2 lock DEFERRED (reviewed 2026-06-09). Result `spanning_AUC_W2_2026-06-09.json` (commit `0599ca1`) verified. M3 divergent: single-ε β₀ ratios >1 (newcomers>spanning) at all four ε* BUT integrated AUC ratio <1 (spanning>newcomers, ~0.938, stable). RA scrutiny: single-ε rests on **tiny residual counts** (β₀ 2–48 of 8,000) at near-merged scales; AUC over [0,ε*] is bulk-dominated by the trivial near-zero region; **no significance test on any statistic** (single matched draw). W₂=2.536 directionless. Headline not robustly supported either way. **User decision 2026-06-09: re-analyse with inferential grounding → T1.9b.** Result retained as point-estimate input; NOT merged; §6 not locked. | tda-agent-3 | run/spanning-betti-robustness |
| 1.9b | **MERGED PR #51 → `main` 2026-06-20 (merge `ad4aaef`); CodeRabbit findings addressed (`49a4603`: path scrub on T1.9 result, decision.consistent recompute enforcement, --matched-n guard, 2 new mutation tests) + Codacy 3 security = benign Bandit subprocess FP (adjudicated); worktree `run-spanning-betti-robustness` **SWEPT 2026-06-20** (Manager 11, User-triggered) — working tree clean (only stdout/stderr checkpoint logs, no uncommitted deliverables); merged local branch `run/spanning-betti-robustness` deleted (was `49a4603`).** Prior: Done — reviewed + outcome-locked 2026-06-20 (Manager 11).** Success; RA-validated against the committed JSON `decision` block + the 2026-06-09 pre-reg (not the report). Exact pre-registered run (no shortcuts): frozen PCA, matched n=8000/group, seed 42, B=1000 perm + B=1000 boot, exact H0, `gudhi.wasserstein` order=2/internal_p=2; **observed-equivalence gate reproduced T1.9 exactly** (β₀ 30/48,8/12,4/5,2/3; full-AUC 0.9379…; W₂ 2.5362604…; eps_lo 0.1115). Outcome **`newcomers_robust`**: windowed normalized β₀-AUC ratio 1.1266>1 @ε*=0.54, perm p=9.99e-4, boot CI [1.0948,1.1727] excludes parity, **direction-consistent across all four ε\***; **dual-metric** (windowed-AUC + W₂, p=9.99e-4) satisfied. **Resolves the M3 divergence** (T1.9 full-AUC<1 was trivial-region bulk-domination; windowed statistic isolates discriminating scales). Single-ε (1.6, p=0.025 @ε*=0.54 only) corroborative-only. **§6/§3.4.2 [DECISION] FILED** (top of Computational-Log): P01-A §6 / P01-B §3.4.2 may claim newcomers carry more H0 structure (robust, inferential); single-ε disclosed as corroborative. Worker filed [RESULT] only (lock withheld for Manager — correct). Unblocks T2.15/T2.17 prose (Stage 2). Prior: **Active — follow-up dispatched 2026-06-11** after pre-implementation Partial: Worker benchmarked the exact computation (~51s/draw: 2×H₀ ~27.5s + exact W₂ ~23s; lanes ≈ 28h serial) and correctly refused approximation shortcuts (pooled-component compression / full-pool-MST induced labels / death-time resampling = different null/statistic). **Manager determination: exact pre-registered run APPROVED, no amendment** — 2,000 draws (1,000 perm + 1,000 boot) ≈ ~7h at 4 workers; memory ~2–2.5GB/worker peak (W₂ cost matrix) fits 32GB. Follow-up adds execution mechanics only: unified per-draw computation (all 3 stats from full H₀ diagrams), observed-equivalence gate vs T1.9 committed values (β₀ 30/48, 8/12, 4/5, 2/3; AUC 0.9379…; W₂ 2.536260410018281) before lane launch, SeedSequence(42) per-draw spawns, ≥4-worker single reusable executor, checkpoint_every≈25 + resume, per-draw stat retention in committed JSON, pause-for-direction if projection >12h@4w. Original design unchanged: label-perm null B=1000 two-sided (r+1)/(B+1) + within-group bootstrap B=1000 on single-ε ratio / windowed-normalized AUC (β₀(ε)/n over [ε_lo, ε*]) / W₂; decision newcomers_robust / spanning_robust / no_robust_difference at locked ε*=0.54, dual-metric. Pre-reg amendment `pre_registrations_2026-06-09.json` + contracts `spanning-betti-inference-output{,-json-validation}` (`ee17195`). Same worktree/branch as T1.9. §6/§3.4.2 [DECISION] is Manager's, post-review. | tda-agent-3 | swept 2026-06-20 (was run/spanning-betti-robustness) |
| 1.10 | **Done — merged 2026-06-08 (PR [#40](https://github.com/stephendor/TDL/pull/40) → `c9f76db`).** Artifact verified `sub_regime_thresh_sweep_2026-06-07.json`: 358/134/40 sub-regime nodes at abs(z)∈{1.0,1.5,2.0}; 40 high-confidence (BH-sig at all three). B=1000, seed=42, (r+1)/(B+1), BH-FDR; reused canonical Mapper graph (kmapper absent — transparent). Descriptive robustness, no prose lock. Gate 62/62. Unaffected by the T1.11 density-peak bug (different code path). | tda-agent | swept 2026-06-20 (was run/stage1-aux-diagnostics; uncommitted T1.11 `[::-1]` density-peak inversion fix recovered → main `50077bc`) |
| 1.11 | **Done (corrected) — merged 2026-06-08 (PR [#40](https://github.com/stephendor/TDL/pull/40) → `c9f76db`).** CodeRabbit caught a **density-peak inversion** (`[::-1]` on the lexsort selected the k LOWEST-density modes). Fixed (`5451fbf`) + **re-ran** → `sublevel_kde_h0_2026-06-08.json` (buggy 06-07 removed). Corrected **ARI = 0.0, single cluster**: the 7 highest-density modes are co-located singleton basins (duplicate embedding points, log-density −12.39) and the 20D KDE mode-seeking fragments into 6,030 basins (largest 5%). Mechanism verified directly. **Conclusion (mechanistic): the KDE density-gradient partition yields NO usable density-mode complement in 20D — diagnostic degeneracy, not evidence about regime structure.** Record as limitation/disclosure only; possible further guidance at paper pre-review. Pre-reg ARI>0.3 not met. Gate 62/62. | tda-agent | swept 2026-06-20 (was run/stage1-aux-diagnostics; uncommitted T1.11 `[::-1]` density-peak inversion fix recovered → main `50077bc`) |
| 1.12 | Done | panel-statistics-agent | run/bic-ipw-mice-income |
| 1.13 | Done | panel-statistics-agent | run/bic-ipw-mice-income |
| 1.14 | Done | panel-statistics-agent | run/bic-ipw-mice-income |
| 1.15 | Done | panel-statistics-agent | |
| 1.16 | Done | panel-statistics-agent | |
| 1.17 | Done | panel-statistics-agent | |
| 1.18 | Done | panel-statistics-agent | |
| 1.19 | Done | panel-statistics-agent | |
| 1.20 | Done | panel-statistics-agent | run/tier2-regression |
| 1.21 | Done — superseded 2026-05-25. Original Tier 3 cross-classified spec structurally non-estimable (~92% FOO singletons); [NEGATIVE] vault entry filed. Replaced by T1.33+T1.34+T1.35 per Spec amendment. Diagnostic work preserved on `run/tier3-regression` (commit `ddc7efb`) as historical record. **Worktree SWEPT 2026-06-20 (Manager 11): branch pushed to `origin/run/tier3-regression` (`ddc7efb`) to preserve the [NEGATIVE] artifacts off-machine, then local worktree + branch removed.** | panel-statistics-agent | swept 2026-06-20 (origin/run/tier3-regression retained) |
| 1.22 | Deferred 2026-05-25 — formal mediation (Baron-Kenny / Imai-Keele-Tingley) out of scope for this paper per Spec amendment; descriptive cross-tab folded into T1.34. | panel-statistics-agent | |
| 1.23 | Done — **merged 2026-06-07 (PR [#37](https://github.com/stephendor/TDL/pull/37) → `7c6bcd8`)**. RA 4-lane pass. Observed ARI 0.2425 ≈ **exact** fixed-margin max 0.2425 → normalised ARI 0.99999 (H0=11,315 comps at ε*=0.54; 10,183 singletons). Null B=5000 p=2e-4; bootstrap CI [0.2370, 0.2485]. CodeRabbit batch addressed `c6310f5` (research-context header, PROJ_ROOT env override, YAML BOM strip; declined two-path-convention false-positives w/ reason). Worktree+branch swept. | panel-statistics-agent | |
| 1.24 | Done | panel-statistics-agent | |
| 1.25 | Done — **merged 2026-06-07 (PR [#39](https://github.com/stephendor/TDL/pull/39) → `cb0564d`)**. Artifacts verified (pre-reg + result JSON), RA 4-lane pass, outcome-contingent rule applied correctly. **Both collapses BELOW ARI 0.9** (U→I 0.7022, U→E 0.6280 vs locked 9-state) → **sensitivity disclosure, no robustness claim** (pre-reg branch `one_or_both_below_0_9`). Outcome-lock [DECISION] filed; Spec §"Sparse U-states" corrected. CodeRabbit batch addressed `d3a9de6` (run-date default→today, vacuous-pass test guard; declined two-path/immutable-artifact false-positives w/ reason). Worktree+branch swept. | tda-agent | |
| 1.26 | **Done — §6.2 resolved via T1.26b; in PR [#45](https://github.com/stephendor/TDL/pull/45).** Original run (`bhps_only_gmm_ph_2026-06-09.json`, `5a6f4dd`): H₀ preserved on both metrics; H₁ metric-divergent (W₂ flips to non-reject, landscape L² still rejects) at n=3,202 / 100% landmark fraction. Ruled INCONCLUSIVE 2026-06-09 (W₂-only `overlap_dependent` [DECISION] withdrawn); retained as valid input data. Superseded by the T1.26b outcome. | panel-statistics-agent | |
| 1.26b | **Done — reviewed 2026-06-13 (Manager 10); RA 5-lane PASS vs artifacts; in PR [#45](https://github.com/stephendor/TDL/pull/45).** Outcome `size_or_fraction_artefact`, `overlap_specific=false` (`bhps_nonoverlap_reanalysis_2026-06-09.json`, completed 06-13, commit `1b93141`): Arm A no H₁ W₂ reject at matched L=1882 (p=0.221) but remainder d_perm=−2.8545 **inside** K=20 same-size full-BHPS band [−2.971,−0.706] (not boundary). Dual-metric corroboration: landscape L² rejects in Arm A (p=0.020) + all 20 controls (W₂ rejects 0/20). Remainder value byte-identical to T1.26 committed JSON (linchpin verified). Contracts pending→active, gate 68/68. **§6.2 [DECISION] filed in Computational-Log** (cross-era robustness SUPPORTED; W₂ reversal = disclosed size/landmark-fraction artefact). Unblocks T2.8. | panel-statistics-agent | |
| 1.27 | **Done — merged 2026-06-08 (PR [#41](https://github.com/stephendor/TDL/pull/41) → `950b4b2`).** Artifact verified `gmm_10of14_2026-06-07.json`: N=25,824, ARI=0.5974 vs locked regimes on 25,343 common pidps → **low-ARI pre-reg branch** (presence rule materially shapes regimes; flag Stage-4 Option A as consequential, temper regime-stability). Frozen scaler/PCA-20 (svd full) applied transform-only; labels from `05_analysis.json["gmm_labels"]`; k=7 full-cov GMM, 5 starts, seed=42. One PCA sign-flip on comp 13 (immaterial to clustering). Denominator note: 0.265 of broad eligible (97,269) but 0.5065 of wave-a baseline (50,982) — meets ~45–55% target. CodeRabbit fixes `38a53d5` (research-context headers, docstrings, env override, POSIX checkpoint path; declined `__file__`-PROJ_ROOT + drift-guard with reason). Binding 3/3, gate 58/58. | panel-statistics-agent | run/sample-10of14-gmm (unswept — fresh fix commits, CodeRabbit re-review pending) |
| 1.28 | **Ready 2026-06-16 (tda-agent free after T1.8; scope User-approved 2026-06-13); agent Panel→TDA; hold/stagger after T1.5+T1.9b per the T1.8 contention lesson.** Recompute §6.1 per-subgroup Markov-1 W₂ under the locked pipeline (frozen loadings, B≥1000, mean-vs-mean T_ratio + BCa) for **USoc AND BHPS**, then per-family FDR (BH gender/NSSEC, BY cohort). Legacy `06_stratified.json` (B=100, pre-frozen, pairwise-between-subgroup) preserved, not citable. **Pre-reg filed 2026-06-13** (vault Computational-Log + `results/panel_methodology/fdr/pre_registrations_2026-06-13.json`); **§B8 read → test model pinned: 50 per-subgroup tests = 2 gender + 6 NSSEC + 42 cohort (one test per subgroup, NOT pairwise)**, BH gender/NSSEC + BY cohort. Contract specs ready (`stratified-w2-recompute-output`, `stratified-w2-fdr-families-output`, `stratified-w2-fdr-output-json-validation` — author YAMLs on the branch at dispatch). **Dispatch-ready.** ~50 subgroups × 2 datasets → heavy. Dep T1.37 (done). | tda-agent | |
| 1.29 | Done | panel-statistics-agent | |
| 1.30 | Done | panel-statistics-agent | |
| 1.31 | Done | panel-statistics-agent | |
| 1.32 | Done — merged 2026-05-16 (commit 11eb993). Groups A (IPW SMD/overlap, AUC=0.7137, ESS=57028.2), B (NSSEC skip rule, 2228 skipped/1773 propagated, L rel_diff=10.3%), C (mice_income convergence rename), D (13 script/style fixes), E (3 shared script path fixes). | panel-statistics-agent-2 | |
| 1.33 | Done — **merged 2026-06-02 (PR [#30](https://github.com/stephendor/TDL/pull/30) → merge `90da1d4`)**. Reviewed (Manager 6), all 5 RA lanes pass. Outcome **SUPPORT + SIGNAL_NOT_TOPOLOGY_SPECIFIC** (topological arm p=1/5001, all 3 ICC CIs >0; C1 raw-bigram + C2 summary arms both also reject under identical machinery). [RESULT]+[DECISION] on file at top of Computational-Log; 3 result JSONs + producing script + binding tests on main. Reviewer-feedback commits (none affecting the result; committed JSONs take unchanged code paths): Codacy `9726360` (`K_GRID` mutable-default → `None` sentinel; Bandit test-tree exclude); CodeRabbit `f144351` (integration marker + narrowed JSON scan) + `d103648` (`PROJ_ROOT` env-override + `WORKTREE_ROOT` rename; `effect_size_ratio` inf→1.0 JSON-safe; declined p-value-helper refactor as correct/contract-bound). Worktree `run-foo-topology-signature` **retained** per CodeRabbit removal cadence. | tda-agent | |
| 1.34 | **DONE — T1.34a-redo landed via the pre-registered svyglm fallback 2026-06-03; tracker row was stale.** The WeMix::mix survey pseudo-likelihood mixed model FAILED its bounded feasibility gate (`e019c1c`), so per the 2026-06-03 amendment's fallback clause the headline reverted to design-based svyglm (`tier2_svyglm_headline_2026-06-03.json` + `tier2_ipw_mice_svyglm_2026-06-03.json` on main; branch `run/foo-regression-transparency` merged PR #31). Estimand unchanged; §4.5 headline = svyglm + cluster-robust SEs (gates T2.4). — Prior (stale) state: Partial — reviewed 2026-06-02 (Manager 6); RA Statistical/Panel lane applied + artifacts verified. **T1.34b** cross-tab done (N=7275, no sparse cells); **T1.34c** full-sample FOO sensitivity done (B=1000, 1000/1000 finite; σ_foo=0.1702, CI [0.0001, 36.9459] — strictly >0 per rule but very fragile/wide; S8 = detectable-between-FOO-variance-with-disclosure). **T1.34a headline BLOCKED:** registered normalised-IPW + household-RE `glmmTMB` is pathological (ICC 0.062→0.997, regime_initR6 3.40→11.7, NS-SEC L sign-flips) — diagnostic isolates it to the weights×RE glmmTMB combo (glmmTMB `weights=` are precision, not survey, weights); unweighted GLMM (ICC 0.062) + design-based svyglm both stable. Worker correctly refused to silently substitute svyglm. **RESOLVED 2026-06-03 — User chose Option B (survey pseudo-likelihood mixed model);** pre-reg amendment [DECISION] filed at top of Computational-Log (withdraws weighted-glmmTMB headline; locks WeMix::mix-preferred / svylme-fallback approach with in-Task feasibility verification; estimand unchanged; acceptance rule = converges + non-degenerate ICC + regime_6 OR>1 + NS-SEC direction matches T1.20; fallback to svyglm+unweighted-GLMM if pseudo-likelihood also non-estimable). **T1.34a-redo dispatched 2026-06-03** (batch with T1.35). [RESULT] (partial) on file. Committed `8c42abe`; **not merged** (held until T1.34a-redo lands, then merge the T1.34 set together). | panel-statistics-agent | run/foo-regression-transparency |
| 1.34a-redo | **Partial** — reviewed 2026-06-03; Phase-1 feasibility only. **WeMix non-estimable:** 500-hh subset fails `Matrix seems negative semi-definite`; full first-imputation >4001s (>1h); >22h serial / >5.5h ×4-worker for m=20. Root cause = household RE over ~6,037 hh / ~7,097 indiv (mostly singletons) → variance component non-estimable — the SAME degeneracy that exploded the original glmmTMB (not specifically a weights bug). Pre-reg **fallback (Option A: design-based svyglm headline + unweighted-GLMM ICC companion, household variance reported as not reliably estimable)** triggered by the amendment's non-estimability decision rule. **DONE 2026-06-03b (batch `34a-fallback` Success, commit `77d9530`):** svyglm headline promoted, `regime_initR6` OR>1 and NS-SEC directions match T1.20 baseline, weighted household variance recorded non-estimable (not an ICC finding); acceptance gate passed. `tier2_svyglm_headline_2026-06-03.json`; binding test added + pending cleared. CodeRabbit fixes applied (normalised-IPW in svyglm design, `level`→null serialization, NS-SEC NA guard, literal params); headline OR unchanged (svyglm invariant to proportional weight rescale), acceptance holds. Committed `e019c1c`/`77d9530`; **DONE + MERGED 2026-06-03 (PR #31 → `7b05805`).** | panel-statistics-agent | run/foo-regression-transparency |
| 1.35 | **Done (corrected)** — reviewed 2026-06-03b (batch `35-redo` Success). Prior Partial 2026-06-03; ran (commit `21c1f0e`), 20/20 tests + 49 contracts pass, but TWO substantive defects block acceptance. **(1) Power simulation statistically invalid:** curve INVERTED (power 0.94→0.84 as ICC 0.01→0.20) and 94% at ICC=0.01. Manager diagnosis: (a) `fit0` is `glm()` but `fit1` is `glmmTMB()` — their `logLik` constants are not comparable, so `2*(ll1-ll0)` is a biased LRT (over-rejects ~everywhere); (b) `pchisq(lrt, df=1)` is wrong for a boundary variance test (needs the ½χ²₀+½χ²₁ mixture); (c) glmmTMB convergence failures at higher σ are silently counted as non-rejections → drives the inversion. `minimum_detectable_icc=0.01` is meaningless. **(2) T1.21 sample non-reproducible:** T1.21 never persisted a PIDP manifest (sample defined procedurally in `regression_tier3.R`); live re-derivation gives 7,109 vs contract-pinned 7,098, reconciled ad-hoc — not provable identity. Also **σ_foo=6.24 (ICC≈0.65) is a separation artifact** on 353 tiny concordant clusters, NOT a real large FOO effect — it is the SAME non-estimability T1.21 hit, not a contradiction. **RESOLVED 2026-06-03b (corrected run, commit `6a2fa72`):** power sim rebuilt (glmmTMB/glmmTMB, ½χ²₀+½χ²₁ boundary mixture, convergence accounting, ICC=0 calibration); T1.21 sample reconstructed deterministically via `regression_tier3.R`. **Corrected engine still not calibrated** (ICC=0 type-I = 0.951) → `minimum_detectable_icc = "engine not calibrated"`, σ_foo=6.23 flagged separation artifact → **S8 = non-estimability framing** (no power claim). **Count discrepancy traced (User-flagged):** reconstruction gives 6,995 / 711 / 342 / 6,284 = the T1.21 fitted-sample deliverable (`tier3_cross_classified_2026-05-25.json` n_obs=6995); the prior 7,098 / 735 / 353 / 6,363 pin was a **pre-complete-case eligibility-stage count** free-typed into the T1.35a pre-reg, undetected because T1.21 persisted no sample manifest. Manager corrected the full T1.35 contract family + fixture to fitted-stage (commit `c3f9598`); `sample_provenance`-ledger rule locked in CONVENTIONS (2026-06-03b) + E6 contract queued in RA plan. CodeRabbit batch addressed (corrected re-run `74351d8` + calibration-gate contract `b9aedd9`; all-pairs concordance n_pairs 342→396). **DONE + MERGED 2026-06-03 (PR #31 → `7b05805`).** | panel-statistics-agent | run/foo-regression-transparency |
| 1.36 | Done — merged 2026-05-26 (commit `7e7ffcb` → merge `753420a`). Frozen scaler/PCA/UMAP threaded through `ngram_embed()`, `_order_shuffle`, `_markov_shuffle`, `_stratified_markov_shuffle`, `_single_permutation`, `permutation_test_trajectories`. Stage-1 phase scripts have `--frozen-loadings` flag (default off — backward-compatible). 4/4 unit tests green; L=500/B=10 USoc smoke canary passed. Label/cohort shuffle redesign remains out of scope (separate audit work-stream). Closes code-review P1-5. | tda-agent | |
| 1.37 | Done — merged 2026-05-29 (merge `97f5026`; branch `run/headline-batch-frozen-pca-rerun` deleted, worktree removed). tda-agent instance 2 delivered: cache denominator cleanup (12 recomputed, 8 LM unrecoverable, 0 cache-layer flips), frozen-loadings reruns (USoc/BHPS headlines, USoc LM L2500/L8000, BHPS length-matched), frozen stratified Markov-1, 50-cell three-layer comparison JSON (21 rejection-direction changes), P01-A/P01-B disclosure notes, vault [RESULT]+[DECISION]. Full APM + research-assurance review (5 lanes) passed; verified directly against artifacts. **Decisions: T1.3 Outcome A SURVIVES** (frozen stratified `outcome=A`; USoc 9/14 BH-FDR ≥0.5; BHPS per-regime mostly drops post-FDR). **T1.2h prose direction NEEDS REVISION** — frozen-loadings fix flipped BHPS H1 W₂ 0.99→0.019 (now rejects) + 3 landscape headline cells to floor; the 2026-05-25 T1.2h asymmetry/era-bound lock (built on buggy per-call-PCA p-values) is invalidated. Surfaced to User as a locked-decision-reversal decision point (gates T2.3/T2.8/T2.16/T2.9/T2.19). | tda-agent | |

## Worker Tracking

| Agent | Instance | Notes |
|-------|----------|-------|
| reproducibility-agent | 1 | idle — T0.13 Done + merged 2026-05-24 (~25min walltime, well under estimate); T0.3 still paused awaiting canary_machine2 file on pipe/two-machine-check |
| tda-agent (slug `tda-agent`) | 2 | **idle/available 2026-06-16 — T1.8 Done + merged + swept** (reviewed Manager 10, RA 5-lane pass; PR #47 CodeRabbit batch `69bb371` addressed → MERGED `df1e737` + worktree swept). Next candidates T1.6 + T1.28 (both assigned here, Ready) — held pending User trigger + compute headroom. Worker Handoff instance 1→2 remains in effect. |
| tda-agent (slug `tda-agent-2`) | — | active 2026-06-11 — **T1.5 follow-up** (W₂ efficiency hardening + production diagnostics) on `run/h2-positive-control` after 2026-06-11 Partial. New concurrent-compute instance; needs cross-agent dependency context (frozen loadings, canonical pipeline) per the tda-agent override below. |
| tda-agent (slug `tda-agent-3`) | — | active 2026-06-11 — **T1.9b follow-up** (exact run approved with execution plan: ≥4-worker checkpointed lanes, observed-equivalence gate, per-draw retention) on `run/spanning-betti-robustness` after pre-implementation Partial. T1.9 reported Success but ruled DIVERGENT/inconclusive on RA review (single-ε tail-noise vs bulk AUC, no significance test); §6/§3.4.2 lock deferred to T1.9b. |
| academic-writing-agent | 1 | idle 2026-06-06 — T2.4 reviewed and merged (`806363b`); §4.5/§S8 prose and claim trace are ready for User section review. |
| panel-statistics-agent | 1 | idle 2026-06-13 — **T1.26b Done** (reviewed Manager 10, RA 5-lane pass vs artifacts; PR #45). §6.2 [DECISION] filed (cross-era robustness SUPPORTED; W₂ reversal = size/landmark-fraction artefact). T1.26+T1.26b await CodeRabbit → merge → sweep. Prior T1.27 merged (PR #41). |
| panel-statistics-agent | 2 | retired — sole task T1.32 Done + merged 2026-05-16 (commit `11eb993`); worktree + branch cleaned up at merge; bus directory `panel-statistics-agent-2/` retained for archive only |

**Cross-Agent Overrides:**
- tda-agent: Tasks T0.4–T0.8, T1.1, T1.2 (incl. T1.2a–T1.2h), T1.3, T1.4, T1.36 (pre-Handoff, instance 1) — treat as cross-agent. Instance 2 (Handoff 1, processed 2026-05-29) loaded only the T1.37 current-stage logs; all earlier tda-agent work lacks loaded working context. Provide comprehensive dependency context when any of these are upstream of a future tda-agent Task.

## Version Control

| Repository | Base Branch | Branch Convention | Commit Convention |
|-----------|-------------|-------------------|-------------------|
| `c:\Users\steph\TDL` | `main` | `pipe/<desc>` for pipeline code-side fixes; `run/<desc>` for computational Tasks; `paper/<desc>` for prose Tasks; `repo/<desc>` for repo-extraction Tasks | `[PREFIX] PXX: <description>` with PREFIX ∈ `{RESULT, DECISION, NEGATIVE, PIPELINE, DATA, EXPLORE}`; Co-Authored-By trailer; pre-commit hooks run (no `--no-verify`) |

> Note: worktree directory names are derived from branch names by replacing `/` with `-`. Branch names remain slash-form for git semantics, while the corresponding `.apm/worktrees` folder uses the hyphenated slug.

## Working Notes

- **2026-06-22 (Manager 11): B9 vintage-coherence audit [User chose "audit first"] → resolved: §4 clustering chain is uniformly Apr-8; the May-2 OM-input file is a divergent orphan.** Reviewer B9.4 requires the FULL normalisation (null SE + max-achievable ARI + normalised ARI + 95% CI), so the point estimate does NOT close it. mtime sweep of the canonical chain: `02_embedding.json`/`embeddings.npy`/`05_analysis.json`(GMM)/`05_om_baseline_results.json`(0.26) **all 2026-04-08, mutually coherent**; frozen §6 batteries (`*_frozen_2026-05-26/28/29/30`) are null-diagram runs **on the Apr-8 frozen embedding**. **Only `01_trajectories_sequences.json` is off-vintage (2026-05-02)** — rewritten by commit `113195d` (2026-05-02 "update embedding parameters"); no canonical result consumes it; the worker's blocked T1.23b fed this orphan to `run_om_baseline` → spurious 0.2062. **Decision: Apr-8 is canonical; published ARI 0.2611807 is the genuine object; NO broader §4/§6 staleness.** Apr-8 OM labeling is unrecoverable from disk (sequences overwritten, `04_om_labels.npy` absent, ward linkage now May-2, baseline holds only scalar 0.26 + GMM margins + *profile correlations* not the count table). **B9 next step = Apr-8 sequence RECOVERY, not a vintage choice:** preferred regenerate from pre-`113195d` code gated by the 0.2611807 canary (fallback: backup restore), then run the full normalisation (perm null B=5000 seed=42, max-ARI, paired bootstrap CI B=1000) on the coherent 0.26 object; quarantine/regenerate the May-2 orphan. **To be dispatched WITH an input-provenance manifest pinning the recovered Apr-8 sequences via the canary** (the new dispatch-readiness gate would otherwise correctly block it — and did, in the demo). Vault [DECISION]/[NEGATIVE] filed (Computational-Log top), supersedes the earlier B9 "decision needed" framing. **B10 remains merge-ready; B9 recompute deferred until Apr-8 recovery is set up.**
- **2026-06-22 (Manager 11): dispatch-readiness gate + compaction-proof PreToolUse hook built (User: "are you losing sight of instructions during compaction? do we need a pre-tool-use hook to ensure vital role responsibilities survive compaction?").** Confirmed the User's two audits on the record: **academic-writing Wave-1 had NO worktree** (reflog shows `checkout: moving from main to paper/p01-stage2-wave1` — the branch was worked in the *main* working dir during a parallel dispatch, the exact isolation the worktree rule prevents); **panel-statistics contracts+worktree were backfilled at 14:41** (`9fbc350`), after the User caught the branch miss — the first envelope had neither. **Root cause = one failure, not three:** the dispatch procedure (`task-assignment.md` §2.5/§3.3) is **lazy-loaded and does not survive compaction** (summary is task-state-focused), and there is no forcing function on dispatch (Worker commits hit the contract gate; the Manager bus-write hit nothing) — so I substituted a confident envelope for the actual setup. **Fix (branch `repo/predispatch-provenance-gate`, commit `613ad44`):** (1) `shared/manager_dispatch_check.py` verifies worktree+`.env` / contracts (validate-only) / input-provenance ledger / cleared report bus **on disk**, emits a `## Dispatch Readiness` block, exits non-zero on FAIL; (2) `.claude/hooks/dispatch-readiness-guard.sh` (PreToolUse, wired in `settings.json`) **blocks a Task Prompt bus write whose Dispatch Readiness block is absent or FAIL** — fires at the write regardless of context, so the discipline cannot be lost to compaction; relays/non-dispatch writes pass; fails open. Docs: §3.3 step 6 + §4.1, hook-enforcement instructions, `.claude/CLAUDE.md`. **Demonstrated:** gate FAILs the panel dispatch on its 2 real gaps (uncleared report bus + B9 incoherent inputs) while confirming the backfilled worktree/.env/contracts; hook 5/5 cases (deny readiness-less + FAIL, allow PASS + relay + non-bus); 4 provenance unit tests green; pre-commit contract gate green. **Hook activates after merge-to-main + settings reload.** Branch now carries both commits (`5ff66f0` input-provenance + `613ad44` dispatch-readiness); ready for User push + CodeRabbit.
- **2026-06-22 (Manager 11): panel-statistics batch reviewed (T1.24b Success / T1.23b Partial-blocked) + pre-dispatch input-provenance gate built (User: "a way to avoid issuing tasks without confirming the data exists in the correct form … needs hooks, contracts or something").** **(1) Task review.** **T1.24b (B10) ACCEPT** — independently re-verified all 7 regime SEs = √(stored·(1−stored)/n_members) within 1e-4 (R0=0.0069 = reviewer worked example), `computed_on:"stability_stored"`, supersedes block correct (`stability_se_stored_2026-06-22.json`, commit `daa3b0d`). Unblocks T2.5 Table 2 ± column; merge on next pass. **T1.23b (B9) PARTIAL, blocker legitimate** — worker correctly refused (no-speculative-paths) to ship ARI 0.2062: regenerating OM k=7 from current on-disk inputs ≠ baseline 0.2611807. **Root cause confirmed on disk (mtimes): `01_trajectories_sequences.json` (OM input) = 2026-05-02, but `05_analysis.json` (GMM labels) + `05_om_baseline_results.json` (baseline) = 2026-04-08** — the OM input was regenerated 3 weeks after the GMM labels it's matched against. Apr-8 baseline holds the scalar 0.2611807 but NOT the 27,280 label vector (only a 7×7 correlation matrix), so the normalisation null needs coherent Apr-8 labels → the overwritten input. **B9 stays blocked pending a User data-vintage decision** (restore Apr-8 build / adopt May-2 + refit GMM / close from committed point estimate — gated on whether reviewer B9 needs the normalised null or just the 0.26 point; I'll read the reviewer-B9 text to settle that before touching vintages). **(2) Manager-failure diagnosis (User: "noticeably sloppier than VSCode … not following established mechanisms").** Precise points: FP-1 verified input *presence/output* not input *vintage coherence* (apm-outputs "Downstream data guarantee" only mandates presence); FP-2 delegated the check downstream by writing "Inputs (verify before compute)" / "Confirmed on disk" into the envelope instead of running a fresh Manager-side check (same class as last turn's branch-setup miss — *substituting a well-written envelope for the pre-dispatch checklist*); FP-3 no mechanical gate on dispatch (worker commits hit the contract gate; Manager dispatch hit nothing). **(3) Enforcement built [User chose full gate], branch `repo/predispatch-provenance-gate` commit `5ff66f0` (8 files, +636; full contract gate green incl. all 82 bindings):** **R-A** Input Provenance Ledger requirement → `task-assignment.md` §2.2 + §4.1 prompt-body section + `apm-outputs.md` mechanics + `contracts/README.md`; **R-B** `shared/manager_predispatch_check.py` (Manager runs at dispatch, exits non-zero on missing/mismatched/incoherent input, output = the ledger); **R-C** `input-provenance-manifest-coherence` *invariant* contract whose binding test re-asserts every `enforced:true` manifest at the worker's commit. **Deliberately did NOT add a 5th contract kind** (README's locked four-kind cap) — rode the existing `invariant` kind + manifest data-files. **Key correctness finding: git does not preserve mtimes** (fresh worktree resets to checkout time; gitignored intermediates absent from worktrees) → two-root rule: committed files signed by `sha256`, `proj_root` gitignored intermediates by mtime-vintage. **Demonstrated:** R-B on the B9 manifest exits 1 flagging exactly the May-2-vs-Apr-8 incoherence (GMM sha256 OK, OM input vintage 2026-05-02 ≠ 2026-04-08). B9 manifest is `enforced:false` until the vintage decision lands. **Not yet pushed** (awaiting User per push-control cadence); CodeRabbit review on PR after push. **Remaining User actions:** B9 data-vintage decision; the two `/apm-4-check-tasks` (panel-statistics merge-ready B10; academic-writing relays still pending).
- **2026-06-22 (Manager 11): completed the panel-statistics compute-dispatch setup the envelope alone had left undone (User caught the gap — same class as Wave-1, where the worker had to self-create its branch).** (1) **Worktree created** (`git worktree add -b run/b9-b10-recompute .apm/worktrees/run-b9-b10-recompute main`, off `4fd826d`) + `.env` copied (mandatory; verified 807 B) + **Workspace section added to the envelope** so the worker computes IN it, not improvising. (2) **Schema-mismatch caught while wiring contracts:** the existing `ari-normalised` schema is **H₀-tree-cut-specific** (requires `eps_star==0.54`/`h0_components`/`embeddings.npy`) and would REJECT an OM-vs-GMM payload — so "reuse the T1.23 schema" (the envelope's original pointer) was wrong; corrected. (3) **Four Manager-authored contracts committed on the branch** (`9fbc350`, `pending:true`, worker writes binding tests + clears pending — per the Manager-authors rule / Obs #7): `ari-om-gmm-normalised-output` (+json-validation) pins `referent`=OM-vs-GMM, `om_k==7`, 27,280 counts, `observed_ari`≈0.2611807, and **forbids `eps_star`/`h0_components`** (object-confusion guard); `stability-stored-se-output` (+json-validation) pins `computed_on=="stability_stored"` + per-regime `SE==sqrt(stored·(1−stored)/n_members)` — **the machine-checkable demonstrator of the new R3 lock**. Meta-schema gate green (85/85). **Dispatch now fully Manager-set-up; awaiting `/apm-4-check-tasks` in panel-statistics-agent.**
- **2026-06-22 (Manager 11): provenance-recording locks applied + B9/B10 recomputes dispatched + Wave-1 relays sent.** (1) **Three governance locks committed on `main` (`0a57f2e`, [DECISION]):** CONVENTIONS **R1** (log every experiment-change as a `[DECISION]` with referent+rationale+supersession+consequence, never a bare new result file), CONVENTIONS **R3** (a result file's self-description fields must be machine-checked against the computed payload — the `stability_se` declared-vs-computed bug), and the `[RESULT]`/`[DECISION]` entry template (`.claude/rules/vault-templates.md`) gains **Referent + Supersedes** fields. Also captured this session: 3 skill observations (#13-15, OPEN), vault permanent note `Provenance-records-the-referent-and-decision-not-just-value-and-date`, memory `feedback_provenance_recording`. Working dir moved to `main` for the governance commit (CONVENTIONS hardlink-safe: identical across branches → checkout didn't rewrite it). (2) **Two recomputes dispatched to `panel-statistics-agent` bus** (branch `run/b9-b10-recompute`): **T1.23b** B9 OM-vs-GMM normalised ARI (regen OM k=7 via `run_om_baseline.py` — npy cache absent, ~min DHD regen; `fcluster(Z,7)-1`; mirror T1.23 machinery; canary observed ARI≈0.2611807; T1.23 H₀-vs-GMM **retained/re-scoped**, supersedes nothing) and **T1.24b** B10 stored-metric stability SE (self-contained from `stability_se_2026-05-16.json`: SE=√(stored·(1−stored)/n_members), R0≈0.0069; output `computed_on:"stability_stored"` — **demonstrates the R3 lock**). Both light, panel-side, may run alongside serial T1.6; contracts Manager-authored per spec, worker writes binding tests only. (3) **Wave-1 relays on `academic-writing-agent` bus:** 2.4-A accepted (§4.5 stands), 2.17-A lock already filed (§4.3 inherits, Betti rerun deferred), 2.18-A scope accepted (§5 stands); **T2.5 stays Partial** until the two recompute JSONs land (finalize §4.6 vs OM-vs-GMM ARI, Table 2 ± vs stored SE). **Awaiting User `/apm-4-check-tasks`** in the panel-statistics-agent chat (recomputes) and academic-writing-agent chat (relays).
- **2026-06-22 (Manager 11): Wave-1 task-review → 4 Success / 1 Partial; 4 experiment-change provenance Decisions/Flags adjudicated via vault audit + User decisions.** Batch returned on `paper/p01-stage2-wave1` (`fbb01e1`→`1534c9c`): T2.4/2.6/2.17/2.18 Success, **T2.5 Partial** (two canonical-file mismatches, surfaced not fabricated). Vault audit of the four changes the User couldn't reconstruct showed **uneven documentation discipline** — spanning T1.9→T1.9b exemplary (pre-reg → 2026-06-09 divergent [RESULT] → 2026-06-20 [RESULT]+[DECISION] lock `acb7f8a3`); the Firth supersession, B9 ARI object-swap, and B10 stability-metric choice were **never logged at change-time** → backfilled in one **2026-06-22 [DECISION]** (Computational-Log top). **(2.4-A) [User: accept loss]** — conditional Firth `tier1_clustered_firth_2026-05-16` (n=6,173) canonical; broad `..._2026-05-13` (n=22,531) **superseded/do-not-cite** (R2 S6 escape-conditioning + locked T1.34 estimand); §4.5 loses the broad-sample class effect (OR 0.835, p=0.0008), class point carried descriptively (cross-tab p=0.7503); §4.5 draft stands. **(2.5-A / B9) [User: recompute OM-vs-GMM]** — T1.23 `ari_normalised_2026-06-06` computed **H₀-tree-cut-vs-GMM** (raw 0.2425, norm 0.99999, null SE 3.48e-4), NOT the §4.6 **OM-vs-GMM=0.26** (confirmed on disk: `results/trajectory_tda_robustness/om_baseline/05_om_baseline_results.json` → `comparison_at_gmm_k.ari=0.2611807`); null-SE 3.48e-4 vs B9's ≈0.009 proves different object. → commission OM-vs-GMM normalised ARI (re-run OM k=7 ~161 s for labels + T1.23 machinery: perm null B=5000 seed=42, exact max-achievable ARI, paired bootstrap CI B=1000); T1.23 H₀-vs-GMM **retained, re-scoped** as a topological-vs-statistical agreement statistic. **(2.5-B / B10) [User: regenerate on stored]** — `stability_se_2026-05-16` declares `use_stability:"stability_stored"` canonical but **computed SE/CI on `stability_from_seqs`** (latent metric bug; R0 SE=0.0024 about 0.5148, not stored 0.2341) → regenerate binomial stored-metric SE (R0≈0.0069, R2≈0.0023). **(2.17-A)** prose-direction lock **already on file** (2026-06-20 [DECISION]); P01-B §4.3 inherits; matched/age-stratified Betti rerun = separate low-priority TDA task. **(2.18-A)** single-machine reproducibility scope accepted until T0.3 completes. **Next:** dispatch 2 light recomputes (OM-vs-GMM ARI + stored stability SE — panel-statistics, can run alongside serial T1.6); §4.6 + Table 2 stay Partial until they land. **T1.6 attempt #4 still running** (~92 h remaining at the ~9 h progress check; measured ~30.3 s/distance; User re-authorized past the 48 h clause).
- **2026-06-21 (Manager 11): Stage-2 Wave-1 prose batch dispatched to academic-writing-agent (parallel with the T1.6 serial compute).** Five independent, locked-foundation sections, zero box contention, no T1.6/T1.28 contingency: **T2.4** (P01-A §4.5 escape regression — svyglm headline per the 2026-06-03 amendment, not the withdrawn glmmTMB), **T2.5** (P01-A §4.6 ARI + Table 2/3 CIs), **T2.6** (P01-A §5 Mapper-vocabulary audit + threshold sensitivity), **T2.17** (P01-B §4.3 spanning — headline = T1.9b `spanning_betti_inference_2026-06-20.json` `newcomers_robust`, NOT the divergent T1.9 point estimate), **T2.18** (P01-B §5 replay-drift→reproducibility — **honesty constraint embedded: T0.3 two-machine check is PAUSED, so no cross-machine bit-for-bit determinism claim; scope to locked-env + lockfile + seed canary**). Batch envelope on `academic-writing-agent` bus (`task-02-04/05/06/17/18`); inputs verified present on main; section-working-file convention (`drafts/sections/`, not new vN); `/notation-check` + per-section User review per section. **Deferred (gated): T2.7 (§6.1←T1.28), T2.8 (§6.2 BHPS←T1.6), T2.3/T2.16 (Wave 2 — safe tables but BHPS-credibility sentence placeholdered ←T1.6), T2.9/T2.19 (synthesis/abstract, last).** Empiricism-first holds: no prose drafted against an unverified result. Awaiting User `/apm-4-check-tasks` in the academic-writing-agent chat.
- **2026-06-21 (Manager 11): Stage-1 status reconciliation + pre-frozen results superseded-manifest (pre-writing-phase housekeeping).** (1) **Three stale "open" rows corrected to DONE** after verifying against artifacts/git (not prose): T1.2g (first13 frozen landed via the 2026-05-30 dedup amendment → Outcome A), T1.9 (superseded by T1.9b merge PR #51 → `newcomers_robust`, §6 locked), T1.34 (T1.34a-redo landed — WeMix feasibility gate failed `e019c1c` → pre-registered svyglm fallback, PR #31 merged). **Net: after T1.6 clears, T1.28 is the ONLY genuinely-remaining Stage-1 task** (heavy §6.1 per-subgroup W2 + FDR recompute; gates T2.7). (2) **Pre-frozen (per-call-PCA bug-era) headline results marked superseded via `results/.../stage1/SUPERSEDED.md`**, NOT physically moved. Reason: the provisional JSONs are live inputs to the regenerable provenance chain — `build_frozen_vs_provisional_comparison.py`, `build_pvalue_denominator_cleanup.py`, `test_stage1_comparison_contracts.py`, and the path records inside `frozen_vs_provisional_comparison_2026-05-29.json` / `pvalue_denominator_cleanup_2026-05-28.json` (no-overwrite-locked). A `git mv` now would break the comparison/cleanup scripts and orphan committed-JSON path references. The manifest clears the writing-phase wrong-pointer hazard without touching provenance; **physical archive (git mv into an `archive/` tree + atomic path updates) is deferred to the paper repo-split**, the natural point where paths get rewritten anyway. Canonical citable headline files are the `*_frozen_2026-05-28/29` set and the dedup `*_frozen_2026-05-30` length-matched cells — enumerated in the manifest.
- **2026-06-21 (Manager 11): "T1.37 reversal still to surface" RETIRED as stale — it was resolved 2026-05-31.** This open item has been carried forward through Manager handoffs (10→11) as if a locked-decision reversal were still pending User adjudication. It is not. Trace: the frozen-loadings fix did flip BHPS H1 (W2 0.99→0.019, landscape-L2 0.080→0.0010) and 20 other battery cells — the 2026-05-25 T1.2h asymmetry/era-bound lock was built on the per-call-PCA bug. But (i) the 2026-05-29 (later) T1.2h [DECISION] superseded the buggy lock, and (ii) the **2026-05-31 Pre-reg #5 redo [DECISION] LOCKED Outcome A** ("§4/§6.2 prose direction unblocked; the BHPS-vs-USoc H1 signal is not a length-of-observation artefact") once the dedup amendment flipped truncate H1 W2 0.350→0.001. The era-bound/horizon framing is not just unpromotable — it is refuted (both surveys reject H1; BHPS H1 survives length-matching both ways). **All frozen numbers are committed on main** (`usoc_headline_frozen_2026-05-28`, `bhps_headline_frozen_2026-05-28`, both length-matched frozen, stratified frozen, the frozen-vs-provisional + dedup comparison JSONs). **P01-A `_project.md:123` already marks it `[x]` closed.** The "gates T2.3/T2.8/T2.16/T2.9/T2.19" tag is what kept it alive in handoffs — but those Stage-2 Academic-Writing prose tasks are gated on full Stage-1 completion (notably **T1.6, running now**), not on any unresolved T1.37 decision. **Genuine residuals (both minor, non-blocking, P01-A `_project.md:133`/`:143`):** (a) auto-thresh pinned-thresh probe on the T1.36 BHPS headline *frozen* cell (compute; defer — would contend with T1.6); (b) SI dedup-disclosure paragraph — **DRAFTED this session** (`papers/P01-A-JRSSA/drafts/sections/supplement-S6-length-matched-dedup.md`, pending User review). **Action for future Managers: do NOT re-list "T1.37 reversal to surface" as an open decision point.**
- **2026-06-21 (Manager 11): T1.6 escalation reviewed → root-caused → re-dispatch authored + process gate added.** Worker did the right thing: bounded `--benchmark-only` probe projected 13.77 h @ 4w, tripped the 12 h guardrail, STOPPED + escalated (no fabricated verdict). **The cost was knowable at dispatch and wasn't costed:** the dominant term is the *retained* observed-to-bank distance count `n_trials=100 × bank_B=200 = 20,000` exact-W2 evals at L=5000 — the dispatch reasoned only about the *avoided* fresh PH ("near-free via cache reuse") and never multiplied the distance evals. Per-eval 2.4–7.7 s is in-band for gudhi-exact (verified `vectorisation.py:226`); the greedy fallback is an approximation we don't want → no free solver lever. **Fix (User): bank_B 200→100 (locked floor) + workers 4→8 (20 cores) → ≈3.4 h.** Re-dispatch on `tda-agent` bus; awaiting `/apm-4-check-tasks`. **Process gate added to `task-assignment.md` §2.2 ("Cost-model gate"):** size stochastic Tasks by `(#statistic evals) × (per-eval cost) / workers` — the product of *every* multiplicative design parameter — not by the count of expensive objects generated; cache-reuse that skips generation does not make a Task near-free if it still runs many evals on the cached objects; require a `--benchmark-only` probe at dispatch when per-eval cost isn't on file. Reinforces [[feedback_spike_benchmark_compute]]. Solver finding reconciled: this code path is gudhi-exact only (no hera), so the "gudhi 3× faster than hera" memory doesn't apply as a lever here.
- **2026-06-20 (Manager 11): worktree sweep — 6 swept, 2 retained; 2 deliverables recovered before removal.** User-triggered sweep of all closeable APM worktrees. **Swept 6:** run-h2-positive-control (T1.5), run-spanning-betti-robustness (T1.9b), run-sample-10of14-gmm (PR #41), run-strand-comparison (PR #49), run-stage1-aux-diagnostics (T1.10/T1.11), run-tier3-regression (T1.21). **Retained 2 per User:** pipe-two-machine-check (T0.3), run-bhps-markov1-credibility (T1.6 active). **The pre-removal gate caught 3 things the "sweep everything" assumption missed:** (1) the h2 worktree held an **uncommitted doubled-n W2 robustness deliverable** (H2 closeout) → scrubbed + committed to main `cb951eb` + vault [RESULT]; (2) `run_stage1_aux_diagnostics.py` held an **uncommitted complete fix for the T1.11 `[::-1]` density-peak inversion bug that was STILL LIVE on main** (line 353) — main carried buggy code + a corrected result the buggy code could not reproduce → fix recovered, validated (ruff + 7 stage1-aux tests), committed `50077bc` + vault [PIPELINE]; (3) `run/tier3-regression` carried **unmerged [NEGATIVE] T1.21 artifacts** (result JSONs + 4 R scripts) on a local, never-pushed branch → pushed to `origin/run/tier3-regression` to preserve off-machine, then removed. Merged local branches deleted for all 6 (tier3 deleted locally, retained on origin). 3 non-APM `.codex/` detached worktrees remain (not Manager-owned). **Lesson reinforced:** never force-remove a worktree on a "looks done" assumption — the gitignored/uncommitted-deliverable + unmerged-commit gate recovered a paper-robustness result AND a live-bug fix that a blind sweep would have destroyed.
- **2026-06-20 (Manager 11): PR #50 + #51 nitpick-cleaned + MERGED to `main` (User: "address here, get it merged").** Manager applied all CodeRabbit nitpicks/findings directly in the worktrees (no Worker round-trip): #50 `3e3cb97` (fixture paths repo-relative, _distribution_delta np.mean cache; docstrings were already in T1.5f — only the np.mean was a re-review item); #51 `49a4603` (T1.9 result path scrub via byte-safe regex past the no-overwrite hook — numbers byte-identical; decision.consistent recompute enforcement + --matched-n guard + 2 mutation tests; committed T1.9 file still validates errors=[]). Codacy adjudicated benign on both (all "Security" = Bandit subprocess B404/B603 FP class on fixed-arg git + H2 child-process isolation; verified no shell=True/eval/exec/os.system/pickle.load/unsafe-yaml; precedent PRs #45/#47/#49). Merged via `gh pr merge --merge` (#50→`fdf53dd`, #51→`ad4aaef`); local main ff-synced. **Both worktrees now sweep candidates** (manual sweep, post-CodeRabbit per cadence) — run-h2-positive-control, run-spanning-betti-robustness. **T1.5 + T1.9b fully closed on main.** Remaining active: T1.6 (tda-agent). T1.28 held; T1.37 reversal still to surface.
- **2026-06-20 (Manager 11): T1.5f DONE → PR #50 pushed, awaiting CodeRabbit re-review + Codacy before merge.** Two follow-up cycles: (1) hygiene/grid-fix/conflict (`5047709`) came back Partial — the confirmatory landscape recompute was blocked because T1.5e cached null diagrams but only the top-20 **observed** summary (full obs = 142@L200/245@L300); Worker correctly escalated rather than recompute PH. (2) Authorized observed-only PH regen (2 small-L PH, frozen embedding, scale-equivariance for the 3 normalizers) → `c2fdde2`, `restriction_stands_confirmed`: all 6 old-vs-new-grid cells preserve verdict, obs-regen drift <3e-6. **H2 closeout now fully defensible.** Two dispatch-precision corrections this session (both User-caught, both logged as skill observations 7+8): contract-YAML authorship (Manager not Worker) and confirmatory-rerun scoping (reuse cached upstream, don't restate B=1000). **PR #50 next step: merge after CodeRabbit re-concludes on `f892bac`/`5047709`/`c2fdde2` + Codacy ACTION_REQUIRED resolved** (Worker found no subprocess in multidim_ph.py → likely refactored away; verify on the live re-run). Compatibility lesson for the pipeline: cache full observed diagrams alongside nulls so a downstream-only metric fix never needs fresh observed PH.
- **2026-06-20 (Manager 11): T1.6 dispatched (User trigger) — parallel, worktree `run-bhps-markov1-credibility`.** Built the full Task Prompt from the 2026-06-13 pre-reg (mechanical per the embedded spec): calibration double-null KS-uniformity + null-null CV variance flag → 3-way verdict; suspect→escalate. Manager dependency check passed before dispatch: T1.37 frozen Markov-1 caches (BHPS+USoc `..._2026-05-28.npz`) present at PROJ_ROOT; .env copied to worktree. Embedded cross-agent upstream context for tda-agent inst-2 (frozen-loadings mechanism, canonical mean-vs-mean W₂ T_ratio in `_battery_core`, the 2026-05-25 label-shuffle [NEGATIVE]). **Contract YAMLs authored by Manager on the branch at dispatch** (commit `d95ee9e`, both pending; gate passed 75/75) per the Manager-authors-contracts convention; Worker writes the binding test + clears pending. (Corrected mid-dispatch — the initial prompt wrongly delegated YAML authorship to the Worker; User caught it.) Recommended disjoint-split cache reuse → likely near-free (no fresh L=5000 PH); guardrail to escalate if fresh-draw projection >12h@4w. **Now running in parallel: T1.5f (tda-agent-2, PR #50) + T1.6 (tda-agent). T1.28 still held (heavy; stagger behind T1.6).** Awaiting User `/apm-4-check-tasks` in tda-agent.
- **2026-06-20 (Manager 11): T1.9b reviewed → Done → §6/§3.4.2 [DECISION] filed → tda-agent-3 free.** Verified against the committed JSON `decision` block + 2026-06-09 pre-reg (not the report): rule satisfied, outcome `newcomers_robust`, observed-equivalence gate reproduced T1.9 exactly. M3 divergence resolved (full-AUC was bulk-dominated; windowed β₀-AUC is the discriminating statistic → newcomers carry more H0 structure, robust + W₂-corroborated). Lock at top of Computational-Log; supersedes T1.9 divergent/inconclusive. **VC: branch `run/spanning-betti-robustness` (6 ahead, result `acb7f8a3`) → opening PR now for CodeRabbit; Manager merges after it concludes (not before).** tda-agent-3 now idle. **Dispatch readiness:** T1.6 + T1.28 (tda-agent inst-2, dep T1.37 done) — the "stagger after T1.9b" hold is now cleared (T1.9b done); only T1.5f (tda-agent-2, light small-L re-run) is active. Recommend T1.6 as the next single dispatch on User trigger (lighter; locks P01-B §4.2.3), staggering the heavy T1.28 (50 tests @ L=5000) behind it. T1.37 reversal still to surface.
- **2026-06-20 (Manager 11): incoming handoff (10→11) processed, bus cleared; PR #50 (T1.5 H2 closeout) advanced toward merge.** Live-state delta since handoff: **PR #45 now MERGED** (T1.26/T1.26b — closed); all four worker report buses **empty** (T1.9b still running on tda-agent-3, first to land). **PR #50 OPEN but `CONFLICTING`** + CodeRabbit (19 inline) + Codacy `ACTION_REQUIRED` (10 security, expected subprocess FPs). Triaged: 1 research-relevant finding = **landscape-L2 grid order-dependence** in `run_h2_dispersion_dualmetric.py:_landscape_l2_between_diagrams` — the metric fed the **terminal locked T1.5d/e "restriction stands" decision**. Validated: dx/(n) error cancels in ratio/rank p; the grid-range-from-observed-only can shift landscape p (truncation/resolution); W₂ (unaffected) corroborates the fragility, so very unlikely to overturn — but a real defect behind a no-overwrite locked paper-claim. **User decision: fix-code + cheap B=1000 confirmatory re-run** (option A). Dispatched as **T1.5f follow-up to tda-agent-2** (owns the live worktree/.env/checkpoints): conflict resolution + landscape fix + re-run + CodeRabbit hygiene + Codacy. **Manager does NOT merge until the report is reviewed AND CodeRabbit re-concludes.** Conflict is vs T1.8 α-sweep (PR #47) on main — additive both sides, union merge, α param must survive in `permutation_nulls.py`. Awaiting User to run `/apm-4-check-tasks` in tda-agent-2. **Standing open item still unaddressed: T1.37 BHPS-H1 reversal** (frozen-loadings flip 0.99→0.019 invalidates the 2026-05-25 T1.2h era-bound lock; gates T2.3/T2.8/T2.16/T2.9/T2.19) — surface to User after PR #50. T1.6/T1.28 remain held (stagger after T1.9b).
- **2026-06-16 (Manager 10): PR #47 CodeRabbit batch addressed → MERGED + swept.** All 9 findings verified vs current code (all valid, none skipped), fixed minimally in `69bb371`: summary-JSON machine-absolute paths → repo-relative POSIX (byte-level edit, all numbers byte-identical — locked α=1 p-values preserved) + `_repo_relpath` for portable future runs; dispatch-test non-empty assert; **checkpoint stale-reuse closed via a load-time param guard** (n_null_pairs/k_max/n_points, backward-compatible — chose the guard over a filename-schema change to avoid orphaning the locked per-cell checkpoints); fail-fast on non-canonical L/seed/B; `alpha≥0` guard; docstring formula precedence; CONVENTIONS footer date; purity-test markov2 coverage; 3 Google docstrings. **Verified no finding touched the run outcomes:** the smoothing *code* was already correct (the formula bug was comment-only) and all 8 cells used identical params (n_null_pairs=500). ruff clean + 7/7 contract tests + guards smoke-tested; 4 inline threads resolved + summary comment; CodeRabbit concluded SUCCESS. **Merged `df1e737` (merge commit); main FF'd; redundant uncommitted CONVENTIONS lock edit discarded (lock in history via `a1d9248`); vault↔repo CONVENTIONS hardlink re-created after the merge rewrote the file** (file-ID match `…113583`, `fsutil hardlink list` shows both paths). **Swept:** worktree `run-markov2-alpha-sweep` + branch (local+remote); pre-removal checklist met (summary JSON tracked on main; 8 cell checkpoints present at PROJ_ROOT, regenerable). Retained worktrees: pipe-two-machine-check (T0.3), run-h2-positive-control (T1.5), run-sample-10of14-gmm, run-spanning-betti-robustness (T1.9b), run-stage1-aux-diagnostics, run-tier3-regression (T1.21). Main working tree still carries pre-existing uncommitted `.apm/tracker.md` (this bookkeeping) + `.superpowers` (out of scope) — clear via a future repo PR like #46. tda-agent free; T1.6/T1.28 still held pending T1.5/T1.9b compute headroom + User trigger.
- **2026-06-16 (Manager 10): T1.8 Markov-2 α-sweep reviewed Success → Done → PR #47.** Verified against `markov2_alpha_sweep_summary_2026-06-16.json` (not the report): pre-reg decision rule applied correctly — W₂ rejection directions stable across α∈{0,0.5,1,5} for all four dataset×dim cells (USoc H0/H1 reject; BHPS H0 reject; BHPS H1 fail-to-reject at every α, d_perm≈−1.0…−2.8) → `conclusion_stable=true`, `canonical_alpha=1`. BHPS H1 non-rejection across α **corroborates** the held §6.2 size/landmark-fraction artefact lock. Null formula/seed/frozen-loadings spot-checks pass. Sweep is W₂-only by design (pre-reg decision rule = W₂-direction stability; dual-metric mandate governs the headline test, not this robustness sweep — not a defect). Important finding operational (contention) only. **Provenance gap caught + fixed:** the [DECISION] CONVENTIONS α=1 lock existed only as an uncommitted main working-tree edit (worker wrote it via the vault hardlink → main repo-root inode, never staged) — added the rule to repo-canonical `CONVENTIONS.md` on the T1.8 branch + gitignored the per-cell checkpoint/status JSONs (summary embeds every per-cell number) so they stop dangling post-merge (commit `a1d9248`). **PR #47 open, awaiting CI + CodeRabbit** (do not merge/sweep until CodeRabbit concludes per cadence). **Main working-tree state:** CONVENTIONS.md still shows the worker's identical uncommitted edit (reconcile at merge: discard-then-ff, then re-verify the vault hardlink since the merge rewrites the file); `.superpowers` shows modified (pre-existing, out of scope); 11 α-sweep checkpoints untracked (PR #47 gitignore neutralizes them on merge). **Dispatch:** tda-agent now free; T1.6 + T1.28 Ready but recommend holding/staggering — T1.5 (tda-agent-2) + T1.9b (tda-agent-3) heavy runs still active, and T1.8's own contention lesson warns against concurrent heavy compute on one machine. Awaiting User dispatch trigger.
- **2026-06-13 (Manager 10): wave-2 batch-2 scoped (User-approved); Plan re-scoped for T1.6 + T1.28.** T1.6 → "BHPS Markov-1 credibility under valid nulls" (calibration double-null uniformity + BHPS-vs-USoc null-null CV; geometry KS dropped; label-shuffle premise void per 2026-05-25 [NEGATIVE]); pre-reg AMENDMENT required. T1.28 → TDA-owned recompute of §6.1 per-subgroup Markov-1 W₂ under locked pipeline (frozen, B≥1000, mean-vs-mean+BCa) for USoc AND BHPS, then BH(gender/NSSEC)/BY(cohort); agent reassigned Panel→TDA; per-subgroup test model to be pinned vs §B8 at pre-reg authoring (NOT assumed — legacy file is pairwise-between-subgroup); pre-reg required. Both dep T1.37 (done). Plan `modified` field + dependency graph (T1_37→T1_6/T1_28 edges, T1_28 recoloured TDA) updated. **Fully scoped 2026-06-13: both pre-registrations filed** (vault Computational-Log + JSON: `results/trajectory_tda_bhps/diagnostics/pre_registrations_2026-06-13.json`, `results/panel_methodology/fdr/pre_registrations_2026-06-13.json`), with complete contract specs embedded (`planned_contracts`). **§B8 read in full → T1.28 test model pinned** (50 per-subgroup tests = 2+6+42, one per subgroup, BH gender/NSSEC + BY cohort; legacy pairwise model rejected). T1.6 amends the 2026-05-13 H4 pre-reg (frozen Markov-1 calibration + variance; geometry dropped). **Remaining at dispatch only: author the contract YAMLs on each task branch + Worker writes binding tests; then queue after T1.5/T1.8/T1.9b, staggered.** Both dispatch-ready; neither on the critical path; no compute started.
- **2026-06-13 (Manager 10): T1.26b reviewed Success → §6.2 [DECISION] filed → PR #45 opened (T1.26+T1.26b combined).** Verified against artifacts per the held-lock guardrail: decision rule correctly applied (`size_or_fraction_artefact`, OR-clause via remainder-in-band), dual-metric corroboration confirmed (landscape L² rejects in Arm A + all 20 Arm B controls — the gap that sank T1.26's premature lock), and the remainder statistic byte-identical to T1.26's committed `bhps_only_gmm_ph` H1 W₂ d_perm=−2.854533606986761 (linchpin). Run completed 2026-06-13 (fresh, not stale). §6.2 prose direction LOCKED: cross-era robustness SUPPORTED, non-overlap W₂ weakening = disclosed small-n/landmark-fraction artefact; unblocks T2.8 (per-section User review still required). **PR #45: CodeRabbit batch-1 `e66432c`** (Google docstrings on t126 run/parse_args/main, `_ordered_metadata_column` contiguity guard, `_configure_core_output_roots` comment) + **batch-2 `d6a986f`** (re-review): **(1) repo-wide CI finding — `.github/workflows/ci.yml` pinned Python 3.11 vs the project's `requires-python==3.13.5` + repo-wide `Path.full_match` usage → CI could not test the project; bumped CI to 3.13.5** (my earlier batch-1 `full_match` decline was right on the API but wrong on the runner — CI hardcoded 3.11); (2) t126 `run()` sets `STAGE1_PROJ_ROOT` from `--proj-root` before the first core side effect (launch-marker provenance split); (3) t126 `parse_args` rejects `--n-jobs<4`. Declined a wall-time flag on t126 (single non-resumable headline; convention met by t126b). Ruff clean + 6/6 panel tests pass. CodeRabbit concluded **SUCCESS** (0 new comments after `d6a986f`). **PR #45 MERGED 2026-06-13 (merge commit `452f2dac`); worktree `run-bhps-non-overlap` + branch swept (local+remote, metadata pruned)** after pre-removal checklist (T1.26/T1.26b result JSONs + scripts + contracts confirmed on `main`; worktree clean; leaf re-analyses, no downstream gitignored consumers). Codacy: 3 Bandit `subprocess` false-positives on `_git_head` (`git rev-parse HEAD`, hardcoded/no-shell — benign; optional `# nosec` follow-up). **CI bumped 3.11→3.13.5 — watch the next CI run for pre-existing latent failures surfaced by the correct interpreter.** §6/§3.4.2 lock still held (T1.9b pending). Three TDA follow-ups (T1.5, T1.8, T1.9b) remain staged awaiting User trigger.
- **2026-06-13 (Manager 10, re-init): session resumed; handoff-09 reprocessed, handoff bus cleared.** Confirmed all four active worker report buses (`tda-agent`, `tda-agent-2`, `tda-agent-3`, `panel-statistics-agent`) empty — no reports pending. State unchanged from the 06-11 note below: four wave-2 follow-ups staged/in-flight awaiting User trigger (T1.8 resume, T1.5 follow-up, T1.9b follow-up, T1.26b re-analysis). PROJ_ROOT shows the 06-11 in-flight artifacts still present (T1.8 6/8 cells; T1.26b `bhps_nonoverlap_reanalysis/`). Two days elapsed since handoff — actual run progress to be confirmed when the User returns each report. Guardrails carried forward: do NOT relaunch heavy worktrees concurrently; verify dual-metric complement + inferential grounding before any outcome-contingent lock; §6.2 and §6/§3.4.2 `[DECISION]` locks remain held.
- **2026-06-11 (Manager 10 incoming): handoff 9→10 processed; T1.5 Partial reviewed → efficiency follow-up dispatched; W₂ solver economics established.** Direct benchmark on the production cache (frozen USoc L=5000; finite H1 diagrams 6.0–6.9k points): exact `gudhi.wasserstein` (order=2, internal_p=2) = **7.9s/pair H1 (9.6s BLAS-pinned), 5.0s H0** — measured under concurrent T1.8+T1.26b load (i7-12700F 12C/20T at 32%). `gudhi.hera` agrees to ~7 significant figures (12.48393614 vs 12.48393589) but is **~3× slower (~30s)** at these diagram sizes — hera substitution rejected; canonical exact solver retained project-wide. Doubled-n volume (4,400 calls) ≈ 9–10h serial ≈ ~2.5h at 4 workers → tractable with NO methodology change. T1.5's smoke ~146s/comparison ≈ 11× intrinsic cost → runner/contention overhead; profile step in the follow-up; `read_null_diagram_cache` verified to materialize lists once (npz re-decompression ruled out). **Generalizable W₂ pattern (User-flagged interest):** (a) per-pair W₂ distance arrays are tiny (~100KB for 4,400) — retain them in committed result JSONs so reruns/BCa recomputes never re-pay solver cost; (b) reuse ONE joblib executor across chunks (per-chunk `Parallel()` construction re-spawns workers on Windows); (c) benchmark the bare solver before assuming W₂ is the bottleneck. User may commission a Fable-driven review of upcoming W₂-heavy tasks once T1.5 confirms the pattern — deferred for now per User direction. In-flight observed from PROJ_ROOT artifacts: T1.8 6/8 cells done (usoc α=1, α=5 outstanding); T1.26b Arm A done + Arm B 17/20 subsamples (~2.5h cadence, ~ETA late 2026-06-11); T1.9b not started. **T1.9b pre-implementation Partial reviewed same day → exact-run follow-up dispatched:** Worker benchmarked exact lanes (~51s/draw; ≈28h serial for 2,000 draws) and refused null-changing shortcuts (correct). Manager approved the exact pre-registered computation — ~7h at 4 workers, ~2–2.5GB/worker W₂ peak — with execution mechanics only (observed-equivalence gate vs T1.9 committed values before launch; SeedSequence per-draw spawns; single reusable executor; checkpoint/resume; per-draw stat retention in committed JSON; pause if projection >12h@4w). No amendment. Launch best sequenced after T1.26b Arm B completes (memory headroom).
- **2026-06-07 (Manager 9): Stage-1 wave-1 parallel dispatch (post scope-align).** Scope-align found T1.7 already Done (PR #28), T1.6 needs reframe (User: reframe-then-keep), T1.28 under-scoped (needs v2 TDA recompute, not panel re-FDR). **Dispatched in parallel:** TDA batch **T1.8 + T1.10 + T1.11** on `run/stage1-aux-diagnostics` (tda-agent inst 2) and panel **T1.27** on `run/sample-10of14-gmm` (panel inst 1). For each, Manager-authored pending output contracts + 2026-06-07 pre-registration committed on the branch upstream of the Worker (contracts pass the gate; Worker writes binding tests + clears pending). Both worktrees `.env`-provisioned. **Wave 2 (deferred):** T1.5 (long H₂), T1.9 (spanning Betti), T1.6-reframe (+pre-reg amendment), T1.26 (TDA-coupled), T1.28 (re-scope to TDA recompute + FDR). Leftover sweep candidate: `run-length-matched-dedup-rerun`.
- **2026-06-07 (Manager 9 incoming): handoff 8→9 processed; PR flow restored.** Verified git state: `main`=`origin/main`=`paper/p01a-escape-foo-prose`=`806363b` (T2.4 already on origin/main via M8 fast-forward, no PR). **T1.23 reviewed (Success, RA 4-lane pass) → PR [#37](https://github.com/stephendor/TDL/pull/37)** (run/ari-normalisation → main; awaiting CodeRabbit; not merged). **T2.4 retroactive review → PR [#38](https://github.com/stephendor/TDL/pull/38)**: base = parent `c890a4b` (head==main so a main-based PR would be empty); 806363b already in main so #38 will NOT merge — CodeRabbit findings (if any) go as a follow-up PR into main, then #38 + `review/p01a-escape-foo-base` are closed/cleaned. New origin branches pushed: `run/ari-normalisation`, `paper/p01a-escape-foo-prose`, `review/p01a-escape-foo-base`. No local pre-push hook — the PR-flow guard is GitHub-side branch protection on main; use PRs for all merges. Uncommitted M8 bookkeeping in working tree (`.apm/tracker.md`, `papers/P01-A-JRSSA/_project.md` marking T1.33/34/35 + pre-reg#5 complete) inspected — accurate, kept. **T1.25 reviewed 2026-06-07** (Success; artifacts verified) → outcome-contingent (both collapses < ARI 0.9 → sensitivity disclosure); outcome-lock [DECISION] + Spec §"Sparse U-states" correction filed. **PRs #37 (T1.23) + #39 (T1.25) merged 2026-06-07** (`7c6bcd8`, `cb0564d`) after CodeRabbit batches addressed (`c6310f5`, `d3a9de6`; declined findings documented on each PR as two-path-convention / immutable-artifact false-positives). **#38 closed** (review-only; CodeRabbit clean on T2.4 prose; 806363b already in main). **Swept:** worktrees run-ari-normalisation + run-u-state-six-state-sensitivity; branches run/ari-normalisation, run/u-state-six-state-sensitivity, paper/p01a-escape-foo-prose, review/p01a-escape-foo-base (local+remote). Retained worktrees: pipe-two-machine-check (T0.3), run-tier3-regression (T1.21), length-matched-dedup-rerun. 7 orphaned `.codex/` worktrees remain (not APM-owned). All Workers idle. **Next-ready panel work** (T1.26, T1.27, T1.28) needs RA triage + Manager-authored contracts before dispatch. Outstanding: T2.4 §4.5/§S8 prose per-section User approval.
- **2026-06-06 (Manager 8): Parallel Stage-1 dispatch active.** T1.23 is active with `panel-statistics-agent` on `run/ari-normalisation` / `.apm/worktrees/run-ari-normalisation`: reconstruct H0 tree-cut labels at ε*=0.54 from canonical embeddings, compare to `05_analysis.json["gmm_labels"]`, compute observed ARI, null SE, max-achievable or explicitly upper-bound ARI, normalised ARI, and bootstrap CI. T1.25 is active with `tda-agent` on `run/u-state-six-state-sensitivity` for both sparse-U collapse variants (U->I and U->E) with Manager-authored pending output contracts. T1.27 remains held as a larger re-embedding/GMM sensitivity.
- **2026-06-06 (Manager 8): T2.4 Academic Writing reviewed and merged.** `academic-writing-agent` commit `806363b` adds P01-A §4.5 escape-regression/FOO prose, the narrow §S8 transparency companion, and a 43-row claim-trace table. Review accepted the worker's important finding: the full-sample FOO sensitivity JSON has an over-strong generated narrative, so manuscript/project wording now treats it as fragile disclosure rather than a strong variance-component claim. User per-section approval remains pending.
- **2026-06-05 (Manager 7): Contract-framework-hardening workstream COMPLETE (T0.14–T0.17 all merged); CONVENTIONS single-source resolved.** PR #36 (T0.17) merged → `0beab81`: gates default to **enforce**, E6 glob path-narrowed (PurePosixPath dispatch, Windows-safe), Contract-Quality Gate checklist in all 4 APM guides (.claude + .codex), defective-fixture block test, vault `[PIPELINE]`+`[DECISION]`. **CONVENTIONS.md is now repo-canonical** (committed at repo root, the single source every agent reaches on checkout — no junction/MCP dependence); the vault file is **hardlinked** to it (one inode, `fsutil`-confirmed; symlink fell back to hardlink because Windows Developer Mode is off). CLAUDE.md updated to declare this. **Operational caveat:** a hardlink can go stale if git *rewrites* `repo/CONVENTIONS.md` as a new inode (vs editing in place) — if the vault copy ever diverges, re-run the hardlink (or enable Developer Mode and switch to a symlink, which is rewrite-proof). **Leftover `CONVENTIONS.md.bak` in the vault root** — safe to delete manually (content is in the repo). **Worktree-sweep leftovers (folders locked by a process at removal time, git-deregistered, need manual `rmdir`):** `pipe-finalize-contract-hardening`, `pipe-retrofit-panel-contracts`, `run-foo-regression-transparency`. **Next:** back on the Stage-1 paper-results track — §S8 prose (svyglm IPW headline + unweighted-GLMM ICC companion + non-estimability framing) to academic-writing-agent is the natural pickup; also still open: E6 sample-provenance-ledger now enforces forward (new fitting outputs must carry the ledger).
- **2026-06-04 (Manager 7): PRs #34 & #35 merged + swept; T0.17 (final close-out) dispatched.** PR #34 (T0.16) CodeRabbit (3: docstrings + integration marker + unused imports) → `dece050` → **merged `978f76a`**, worktree swept (folder locked → manual delete). PR #35 (T0.15) CodeRabbit (numeric-sanity on comparison-arm stats + docstring-coverage 67.86%<80% → docstringed 7 diff-touched helpers to 100%) → `077f243` → **merged `45af181`**, worktree swept clean. **All three retrofit tasks (T0.14/15/16) now on main; 0 open PRs.** **Manager pre-verified the combined warn-mode run = 0 hardening findings (1b/1c/2b) across all 52 contracts on `45af181`** → enforce-flip is safe. **T0.17 dispatched** (reproducibility-agent, `pipe/finalize-contract-hardening`): flip warn→enforce; narrow E6 glob to fitting outputs (via file_dispatch); bake pre-dispatch/pre-accept contract-quality checklist into `.claude/apm-guides/task-assignment.md`+`task-review.md` (dual-tree); defective-fixture block test; vault `[PIPELINE]`+`[DECISION]`+CONVENTIONS lock. Legacy `--all-jsons` backlog (length-matched 2026-05-29 exempt + 9 E6 panel fitting JSONs) stays informational, NOT commit-blocking. This is the last task of the contract-hardening workstream.
- **2026-06-04 (Manager 7, T0.15/T0.16 reviewed): both core-Success; grandfather policy locked; T0.15 follow-up + PR #34.** Both retrofits hit residual→0 for their set with hard gates green. **Review caught:** (a) T0.15 wrote *inferred* provenance ("effective full-sample fallback": n_perm=5000, covering_radius=0.0, …) into the immutable historical `bhps_…_2026-05-29.json` to close a gate-4 gap — integrity-risk backfill; (b) T0.16's E6 surfaced the same class — 9 pre-existing panel fitting JSONs lack `sample_provenance`; (c) `icc-cluster-bootstrap` fell through (Manager mis-routed it to Panel; binding is in `tests/trajectory_tda/`). **User decision: grandfather + comply-forward** — historical result JSONs are immutable, never backfill inferred provenance; new provenance contracts enforce only on staged/new outputs (gate-4 is staged-only, so legacy gaps surface only in `--all-jsons` as tracked backlog). **T0.16 accepted/Done → PR #34** (E6 9-file debt auto-grandfathered by staged-only semantics; T0.17 to narrow E6 glob to fitting outputs). **T0.15 follow-up dispatched** (same branch/log): revert the bhps backfill; add a `legacy_exempt` grandfather primitive (meta-schema + gate-4) and exempt that one legacy file (schema stays active for forward); retrofit `icc-cluster-bootstrap`. **Stale report bus note:** reproducibility-agent/report.md still holds the already-processed T0.14 report (not a new dispatch). **Merge sequencing:** PR #34 (T0.16) + T0.15-followup → both merge → T0.17 (enforce-flip + APM-guide contract-quality checklist + E6-glob-narrow + vault/CONVENTIONS lock).
- **2026-06-04 (Manager 7, later): PR #33 merged; worktrees swept; T0.15 ∥ T0.16 dispatched.** PR #33 CodeRabbit batch (4 comments) addressed — Critical `parts` NameError in gate-4 dict validation, gate-1b false-positive tuning (narrow bare "about" + radius 50→200, register 114→112), README `--no-verify` removed, fixture no-op assert fixed (`b72b016`); merge conflict with PR #32 on the dual-tree `schema-contract-design` skill resolved by combining both (lessons + gate rules, byte-identical, `8d1fd34`). **PR #33 merged → `efc9fb5`** (T0.14 on main). **Worktree sweep:** removed + branch-deleted (local+remote) `pipe-contract-framework-hardening` (#33), `research-assurance-lessons` (#32), `run-foo-topology-signature` (#30), `run-foo-regression-transparency` (#31, git-deregistered but physical dir locked by a process → leftover folder needs manual delete). Retained `run-tier3-regression` (T1.21), `pipe-two-machine-check` (T0.3), `length-matched-dedup-rerun`. **5 orphaned non-APM Codex worktrees under `.codex/` flagged** (not Manager-owned). **T0.15 (TDA) + T0.16 (Panel) dispatched in parallel** off `origin/main` `efc9fb5` (has T0.14+#32+#33). Partition is conflict-free by binding-test file: TDA = `tests/trajectory_tda/*` contracts (foo-topology, topology-invariants, stage1, + 6 topology stochastic-tests); Panel = `tests/panel/*` (panel-output, regression-specs, statistical-tests, + 2 panel stochastic-tests) + authors **E6 sample-provenance-ledger**. Both must reach zero residual hardening findings for their set + impact-check committed JSONs (no contract-weakening). Worktrees `pipe-retrofit-tda-contracts`, `pipe-retrofit-panel-contracts`. T0.17 (enforce-flip) waits on both. **Local main branch ref still at 7b05805** (Option B dirty working state; origin/main is ahead at `efc9fb5` — future worktrees branch off `origin/main`).
- **2026-06-04 (Manager 7): Contract-framework-hardening workstream planned + T0.14 reviewed/Done → PR #33.** Per User direction (systemic review of contracts + tighter process so neither role repeats the CodeRabbit-caught defect classes), added Stage-0 workstream T0.14–T0.17 to plan.md (+ dependency graph + modified log). **T0.14 (gate-framework + meta-schema hardening, warn-mode) reviewed + Done** — verified vs artifacts (not just summary): 7/7 fixture self-tests; default warn exit 0 across all 50 contracts (114 hardening findings + pending-debt reported non-blocking → safe to merge); `--enforce` blocks (exit 1); dual-tree `schema-contract-design` byte-identical; meta-schema has `enforced_by`+`derivation`. Worker correctly adapted `X | null` → arbitrary `A | B` union to support live `float | str` (e.g. `minimum_detectable_icc`) — verified-against-current-code, good. Register `contracts/_audit/contract_hardening_register_2026-06-04.md` = retrofit backlog (114 across 7 dirs; largest classes: invariants missing expression/enforced_by, claim↔assertion coverage shortfalls). Commit `8de0f72` → **PR #33 open, awaiting CodeRabbit**. **Merge sequencing (critical path):** PR #32 (panel contract fixes) + PR #33 (T0.14 framework) both need CodeRabbit + merge BEFORE the retrofit dispatches; T0.15 (TDA) needs T0.14 on main; T0.16 (Panel) needs T0.14 on main AND PR #32 merged (overlapping panel contracts). Order: merge #32 → merge #33 → dispatch T0.15 ∥ T0.16 → T0.17 enforce-flip. **PR #32 MERGED 2026-06-04 → `b336544`** (final CodeRabbit comment — pin `calibration_tolerance == 0.03` into the invariant expression — addressed at `425ccac`, contract gate-1 still green; User-authorized merge). **Now only PR #33 (T0.14) gates the retrofit dispatch:** merge #33 → dispatch T0.15 ∥ T0.16 → T0.17. `research-assurance-lessons` worktree now a sweep candidate (after CodeRabbit concludes on the merged PR). Pre-existing gate-4 gap on `bhps_length_matched_truncate_frozen_2026-05-29.json` (missing 6 `length-matched-run-params` keys) recorded in the register for T0.16/cleanup. Worktree `pipe-contract-framework-hardening` retained (PR #33) per cadence.
- **2026-06-03d (Manager 7 incoming; PR #32 CodeRabbit batch addressed).** Handoff 6→7 processed + bus cleared; Manager 7 active on Stage 1. **PR #32 CodeRabbit review handled** (6 inline findings — found via `gh api .../pulls/32/comments`, NOT `gh pr view --json comments` which omits inline review comments; tooling lesson saved to auto-memory). **Root diagnosis:** PR #32 branch (`pipe/research-assurance-lessons`) forked at `79444a2` BEFORE PR #31 merged; `0402213` touched only the 4 skill docs; the 3 contract files in the PR diff were stale (behind-main) content, so CodeRabbit reviewed already-corrected material. **Merged `main` into the branch** → PR diff collapses to the 4 skill docs + contracts match main; auto-resolved finding #5 and the bulk of #1/#6. **CodeRabbit traps avoided (verify-against-current-code):** (a) it suggested enforcing `n==735 && n_clusters==353` — the buggy pre-complete-case counts; correct fitted-stage is `711/342` (used that). (b) it suggested companion `se>=0`/`pvalue in [0,1]` as required numbers — but the committed JSON legitimately carries `se:null,pvalue:null` (unweighted point-estimate reference); pinned types/bounds **allowing null** instead. **Fixes pushed (commits `1e9fd68`, `3c183a3`):** #1 numeric calibration gate (|type_i−α|≤0.03); #6 added `expression:` to the 3 expression-less glmm-power-sim invariants (correct 711/342); #2 power_curve+calibration conservation (validator+test+contract); #3 require regime_initR6 + nssec_proxy rows in svyglm_headline; #4 companion-row pinning (null-allowed). Impact-checked against committed `power_analysis_corrected`/`tier2_svyglm_headline` JSONs (both satisfy new checks); 21/21 panel binding tests + 50 contracts pass. **Worktree `.apm/worktrees/research-assurance-lessons` created** (PR #32 branch) — sweep candidate after merge. **Pre-existing out-of-scope finding:** `--all-jsons` gate-4 flags `bhps_length_matched_truncate_frozen_2026-05-29.json` missing 6 `length-matched-run-params` keys (T1.37-era file on main; not staged, so doesn't block commits) — log for later. **Next:** merge PR #32 after CodeRabbit re-reviews `1e9fd68`+`3c183a3`; then worktree sweep (#30/#31/#32 candidates per cadence); §S8 prose + E6 contract still queued.
- **2026-06-03c (end): PR #31 MERGED (User, manual); review-lessons generalized; PR #32 open.** PR #31 merged to `main` at `7b05805` (User merged manually); local main fast-forwarded (now at/after `7b05805`). **T1.34 + T1.35 fully Done + on main.** One final CodeRabbit comment (calibration gate non-deterministic in the contract) fixed at `b9aedd9` before merge. **Review-lessons generalized:** two recurring meta-patterns from the PR #31 batch — (1) *key-presence ≠ enforcement* (assert value+type; literals equality-checkable; pin gate predicates) and (2) *no silent truncation/coercion/drop + artifact hygiene* — locked as CONVENTIONS ALWAYS rules (2026-06-03c), a Permanent note (`Enforcement-must-assert-value-not-key-presence`), and edits to `schema-contract-design` + `result-provenance-review` skills (byte-identical across `.claude` + `.agents` trees); committed `0402213` on `pipe/research-assurance-lessons` → **PR #32 (open)**. Also filed a third Permanent note (`Variance-component-non-estimable-over-singleton-dominated-clusters`) crystallizing the T1.21/T1.34a/T1.35 non-estimability as one phenomenon. Daily note `2026-06-03` updated. **Worktree `run-foo-regression-transparency` RETAINED** per the CodeRabbit removal cadence (manual sweep only, after CodeRabbit concludes on the merged PR) — do NOT auto-remove. **Open for next Manager:** merge PR #32 after review; sweep the `run-foo-regression-transparency` (and other closed-PR) worktrees once CodeRabbit has concluded; §S8 prose now unblocked (non-estimability framing); E6 sample-provenance-ledger contract still queued in the RA plan.
- **2026-06-03c (later): PR #31 CodeRabbit fix batch — worker re-run reviewed (Success), accepted.** Worker delivered `74351d8` (corrected re-run) + `4bf008f` (AGENTS.md commit-prefix rule, +5 lines, benign); both pushed to PR #31. Verified against artifacts (not just summary): 21/21 tests + 50 contracts; `power_analysis_corrected` carries the corrected σ_u string + 711/342 + `engine not calibrated` (calibration still 0.951 — conclusion holds); `sibling_concordance_corrected` is all-pairs (n_pairs 342→396, n_clusters=342, table (2,14,25,355), κ 0.0603→0.0445, OR 2.44→2.03, McNemar p 0.16→0.11); headline has **zero `level:{}`** (real levels/null emitted), `ipw_trimming` is the literal normalised rule, acceptance `true` (OR 3.5516 unchanged — expected: svyglm point estimates are invariant to proportional weight rescaling; the normalisation governs inference/SE and is now correctly in the design + params); stale pre-correction JSONs removed. All R/validator/test fixes landed. **T1.34 + T1.35 now fully Done** pending the PR merge. Buses cleared. **Next: merge PR #31 after CodeRabbit re-reviews the new commits** (per the CodeRabbit removal cadence — do not merge or remove the worktree until CodeRabbit concludes on the updated PR).
- **2026-06-03c: PR #31 CodeRabbit batch — Manager contract fixes done, R-side + re-run re-dispatched.** CodeRabbit reviewed PR #31 (16 inline + 2 large comments). **Real math bug found:** ICC→σ_u conversion was `icc·π²/3`, should be `(icc/(1−icc))·π²/3` (understates σ_u up to ~25% at ICC=0.20). **Impact assessment: zero change to reported conclusions** — the headline is ICC=0 calibration failure (type-I 0.951) and σ_u=0 at ICC=0 under both formulas; the power curve was already diagnostic-only. **Manager fixed all contract-side findings (commit `4cf4ddf`, pushed):** corrected ICC formula (expression/var/invariant/must_assert + sigma_u_formula pin), encoded the `engine not calibrated` outcome in `minimum_detectable_icc`, fixed stale count strings (7097→6995, 6363→6284), manifest notes (eleven→twelve), and switched the concordance contracts (sibling/mcnemar/kappa) to **all-within-family-pairs** semantics. **User decision (2026-06-03c): enumerate ALL within-family sibling pairs** (was `.SD[1:2]`, silently dropping ≥27 members), family-clustered bootstrap. **Re-dispatched to panel-statistics-agent** (`run/foo-regression-transparency`): R fixes (ICC formula in script, `level`→null serialization in t134/t134a, normalised-IPW in the svyglm design, NS-SEC NA guard, xwavedat provenance path, contract-literal params) + enumerate-all-pairs concordance + **re-run the 3 R scripts** (power sim ≥4 workers) + regenerate 5 JSONs + remove stale pre-correction JSONs + de-skip/tighten validators (711/342 pin, zip length, level-type, test_t134 load committed JSONs). Headline OR + concordance numbers WILL shift (normalised IPW + all-pairs) — worker reports before/after. Task prompt validated OK. Worker does NOT edit `contracts/` (Manager-owned). Merge after worker re-run + CodeRabbit re-review.
- **2026-06-03b: T1.34a-fallback + T1.35-redo reviewed — both Success; T1.21/T1.35 count discrepancy traced + sample-provenance rule locked.** Batch report processed (both Success, committed `77d9530`/`6a2fa72` on `run/foo-regression-transparency`); 21/21 panel tests + 50 contracts pass; vault `[RESULT]`s prepended. **T1.34a:** design-based svyglm IPW headline accepted (regime_initR6 OR>1, NS-SEC matches T1.20, weighted household variance non-estimable). **T1.35:** corrected boundary-mixture power engine is **not calibrated** (ICC=0 type-I=0.951) ⇒ no minimum-detectable-ICC; σ_foo=6.23 = separation artifact ⇒ **S8 non-estimability framing** (correctly surfaced, not a failure). **User-flagged count discrepancy (7,098 vs 6,995) RESOLVED:** stage-of-measurement mismatch — 7,098/735/353/6,363 = pre-complete-case IPW-eligible; **6,995/711/342/6,284 = fitted T1.21 sample** (committed deliverable `tier3_cross_classified_2026-05-25.json` n_obs=6995; worker reproduced exactly). Origin: the T1.35a pre-reg free-typed the eligibility count, propagated into contracts; undetected ~1 week because T1.21 never persisted a sample manifest. **Manager actions:** corrected the entire T1.35 contract family + stale fixture to fitted-stage counts (commit `c3f9598` on the branch; unenforced literals, tests still green); locked the **sample-provenance-ledger** ALWAYS-rule in CONVENTIONS (2026-06-03b); added E6 `sample-provenance-ledger.yaml` + C1 `result-provenance-review` stage-ledger check to the research-assurance plan; filed `[DECISION]` (2026-06-03b) to Computational-Log. **Merge path:** one PR (`run/foo-regression-transparency` → main) carrying all six commits incl. `79444a2` corrected contracts through CodeRabbit; merge after CodeRabbit concludes. Report bus to clear.
- **2026-06-02: T1.34 reviewed (Partial), T1.35 held — open User methodological decision point.** Worker behaved exactly as the RA layer intends: 11/11 contract tests + task-scoped schema validation pass, but it refused to ship the registered T1.34a model-based headline because the weighted household-RE `glmmTMB` is statistically pathological. Verified against artifacts: `tier2_weight_diagnostic_2026-06-03.json` shows unweighted GLMM stable (ICC 0.062, regime_initR6 3.40) and weighted FE logit stable (3.55), but the weighted GLMM explodes (ICC 0.997, regime_initR6 11.7, NS-SEC L sign-flip). **Root cause (Manager assessment): glmmTMB/lme4 `weights=` are precision/prior weights, not survey/sampling weights — feeding IPW (range 0.44–10.86) into a household-RE glmmTMB makes the RE variance absorb the reweighting (this is a known limitation, not a bug). The methodologically correct IPW+clustering estimators are design-based svyglm (already computed, stable) or a survey pseudo-likelihood mixed model (svylme/WeMix).** Decision options surfaced to User (A: promote svyglm as IPW headline + unweighted GLMM as RE/ICC companion, document weighted-glmmTMB failure — recommended, minimal, both artifacts already stable; B: proper survey pseudo-likelihood mixed model — heavier, new dep; C: document weighted GLMM non-estimable [NEGATIVE]-style + report unweighted GLMM + svyglm pair). All three need a pre-reg amendment (registered headline was the weighted GLMM) → significant, User-collaboration per task-review §2.3. T1.34c FOO sensitivity completed but CI [0.0001, 36.9459] is extremely fragile (≈5 orders of magnitude) — "strictly >0" mechanically satisfies the rule but S8 framing is human-review. Branch `8c42abe` held unmerged so the pathological tier2 JSON does not reach main untagged. Report bus cleared.
- **2026-06-02: T1.33 reviewed (Success), accepted, pending merge.** First Task reviewed under the new (merged 2026-06-01) research-assurance layer; applied the full 5-lane RA checklist against the artifacts, not just the Worker summary. **Topology:** local-PH via `per-individual-knn-local-ph` contract + binding test (kNN k+1 sub-clouds, VR maxdim=1, infinite-H0 excluded, finite-H1 summed, landscape grid 256); bespoke construction has no external benchmark → assurance via contract + synthetic tests + non-negativity/schema. **Stochastic/Null:** the critical invariance check passes — features are computed once on the static 90-D embedding and the constrained shuffle permutes *sibling grouping* (preserving the cluster-size partition exactly), so the null is NOT invariant to the tested operation (this is the exact bug-class that invalidated the 2026-05-25 label/cohort-shuffle p-values; here it is correctly designed). p=(r+1)/(B+1)=1/5001 floor, B=5000, rng(42); ICC cluster-bootstrap B_boot=1000. **Representation:** raw 90-D unigram+bigram, no PCA/UMAP/standardize; C1/C2 comparators on same sample + FOO assignment. **Output/Provenance:** date-suffixed JSONs committed on branch, gitignored `.npy` trace at PROJ_ROOT, same-seed byte-identical rerun, SHA-256 + all params recorded. **Paper Claim:** decision-rule outcome follows correctly — both comparators reject with ICC CIs >0 ⇒ SIGNAL_NOT_TOPOLOGY_SPECIFIC (not TOPOLOGY_DISTINCTIVE); honestly framed in the [DECISION]. **Planning impact:** none — this is one of the three pre-registered outcomes with prose direction already locked; no Spec/Plan change. **Unblocks:** academic-writing-agent §4.5.x prose *direction* (the section itself also depends on T1.34). Branch `run/foo-topology-signature` @ `7cf1eb2` is FF-ready on `bc3a548`; merge deferred to PR-based flow per CodeRabbit cadence (offered to User). Report bus cleared.
- **2026-06-01: Manager 6 incoming from Manager 5 handoff; pre-dispatch research assurance + T1.33/T1.34/T1.35 dispatched.** Handoff Bus processed + cleared. Per User direction, research assurance (incl. contract setting) was completed *before* dispatch. **Pre-reg integrity gap found and closed:** the T1.33/T1.34c/T1.35 pre-registrations were filed 2026-05-25 via `vault_observe` only (off-page per the vault-discipline lock) and the vault-engine query index returns no results for ANY page content as of 2026-06-01 (recovery via Option 1 not viable). Re-authored from source (Spec/Plan/Tracker/contracts), filed canonically to the **top** of `04-Methods/Computational-Log.md` as a 2026-05-25 backfill block (top placement, not in the mid-file 2026-05-25 block, because that block is double-encoded and unsafe to edit in place); machine-readable mirror at `results/trajectory_tda_integration/stage1/pre_registrations_2026-05-25.json`. **C1/C2 comparator amendment** (Tracker-recorded 2026-05-25, absent from Spec + contracts) — User confirmed *include*; authored 2 new contracts (`comparator-feature-definitions` formula: C1=raw 90-D bigram coords, C2=9 occupancy proportions + transition count; `topology-distinctiveness-comparison-output` schema), extended `foo-topology-output-json-validation` dispatch, registered both in the T1.33 manifest. All committed on main as `3d19f76` (`[DECISION]`; staged only my files — User's dirty IDE files untouched; gate-1 validation passed across 47 contracts). Worktrees `run-foo-topology-signature` + `run-foo-regression-transparency` (re)created from `3d19f76` with `.env`; T1.33 branch renamed to match its manifest. Both Task Prompts carry `Research Assurance Requirements` blocks. **Research-assurance forward-direction thread (the `.apm/memory/plans/` work) still awaits User's queued context** — not started.
- **2026-05-29: T1.37 reviewed (Success), merged, closed.** Full APM + research-assurance review (Topology / Stochastic-Null / Representation / Output-Provenance / Paper-Claim lanes) — verified directly against the comparison/stratified/headline JSONs + vault entry, not just the Worker summary. Merged `run/headline-batch-frozen-pca-rerun` → main as `97f5026` (clean; run-branch and main touched disjoint file sets); worktree removed, branch deleted. **Headline impact: the frozen-loadings fix moved 4 headline cells toward rejection — most importantly BHPS H1 W₂ 0.99→0.019 (now rejects); 3 landscape headline cells went to floor.** Consequences: (1) **T1.3 Outcome A SURVIVES** (frozen stratified `outcome=A`; USoc 9/14 BH-FDR ≥0.5; BHPS per-regime mostly drops out post-FDR, USoc now carries Outcome A). (2) **T1.2h prose direction NEEDS REVISION** — the 2026-05-25 [DECISION] (BHPS H1 asymmetry / era-bound non-promotable) rested on buggy per-call-PCA p-values; frozen loadings reverse it. Locked-decision reversal affecting Stage 2 prose (T2.3 §4.3, T2.8 §6.2, T2.16 P01-B §4.2, T2.9/T2.19 abstracts) — **surfaced to User as a decision point** before re-locking prose direction or dispatching dependent prose. Worker drafted P01-A/P01-B disclosure notes (`papers/P01-*/notes/methodological-disclosure-frozen-pca-2026-05-29.md`) as raw material. Corrected Stage-1 headline numbers are now canonical on main. Main not pushed since the merge.
- **2026-05-28: Manager 5 incoming from Manager 4 handoff.** Handoff Bus processed (Handoff Log `handoff-04.log.md` + T1.37 artifacts read) and cleared. **State at init:** Stage 1 active; T1.37 is the live coordination focus. `tda-agent` is instance 2 (Worker-side Handoff 1→2 already executed). Corrected frozen jobs 01–03 committed on `run/headline-batch-frozen-pca-rerun` (`2d1b21b` denominator cleanup + contracts, `f3ae454` frozen headline + L2500); **job-04 USoc LM L=8000 frozen still running** (PID 29600 alive, started 2026-05-28 14:25:19, phase-status at START, output JSON not yet present). Worker `report.md` is **empty** — no Manager review pending; T1.37 remains active. Remaining T1.37 work after job-04: job-05 BHPS length-matched truncate, job-06/07 stratified Markov-1, job-08 comparison JSON, P01-A/P01-B disclosure notes, vault `[RESULT]`+`[DECISION]`, final report. Review must apply the research-assurance lane checklist (Topology, Stochastic/Null, Representation, Output/Provenance, Paper Claim) per `.apm/memory/plans/2026-05-28-apm-research-assurance-integration.md` — software tests alone are insufficient. **Observed (not acted on):** main worktree dirty with Repowise/tooling cleanup (do NOT commit unless User asks); two orphaned detached Codex-session worktrees under `C:/Users/steph/.codex/worktrees/` (`1702`@`753420a`, `d07f`@`eff0e7634`) flagged for User; `pipe/two-machine-check` (T0.3) and `run/tier3-regression` worktrees retained as before.
- **2026-05-27 (later): contracts framework + Stage-1 corrective fix landed on main.** Two `[PIPELINE]` commits merged to main via fast-forward: `bb1b0a1` (contracts framework + pre-commit hook gating) and `9c81311` (Stage-1 W2 p-value denominator + LM schema corrective fix, with the 3 binding tests and pending-flag clearance on the affected contracts). All six T1.36 contracts now active; pre-commit hook gates every commit via `.git/hooks/pre-commit` (installed via `install-git-hooks.py`). Smoke pass surfaced and fixed two framework design issues during Phase B: persisted JSONs wrap the function return under a `result` field (added `wrapper_key` to `output_validation` for descent) and `applies_to_glob` was too broad (made `file_dispatch` a positive filter; pre-fix legacy JSONs deliberately not in dispatch scope, addressed by separate T1.2 cascade recompute). Three follow-ups remain: vault `[PIPELINE]` entries for both commits at `04-Methods/Pipeline-Overview.md` + a methodological `CONVENTIONS.md` entry for the framework as a locked process; T1.2 cascade recompute (recompute corrected p-values from cached `.npz` null distributions + draft methodological-disclosure paragraph); T1.37 reissue (fresh worktree recreated from current `main` at `686508a` after User chose remove/recreate; tda-agent Handoff instance 1 → 2 staged).
- **2026-05-27: Manager 3 initiation + math-correctness enforcement framework design session.** Manager 3 incoming from Manager 2 handoff (M2 → M3 transition processed; Handoff Bus cleared). T1.37 corrective-stop state confirmed via Worker log: jobs 00–04 ran with buggy `n_null_pairs+1` p-value denominator (should be `(r+1)/(B+1)`) and LM output truncated `T/d/mean` fields; Worker patched `_battery_core.py` + `run_stage1_battery.py` in worktree + added regression tests (uncommitted as of M3 init). User opened the math-correctness enforcement design discussion (the major thread M2 deferred). Settled: contracts framework lives at `contracts/` (repo root, APM-agnostic, travels with worktrees); pre-Worker extraction agent authors contracts; structured YAML grammar locked to 4 kinds (formula, schema, invariant, output-validation); one contract → one binding test; hook runs all bindings on every commit; ambiguities surface for review; pre-regs generate fresh contracts; project-wide scope; hard-enforced from day one. Six-trigger model: (1) Plan authoring, (2) pre-reg authoring, (3) Manager pre-dispatch coverage check, (4) pre-commit hook forcing function, (5) Spec amendment, (6) in-chat / post-hoc finding. **T1.2 cascade decision:** recompute corrected p-values from cached `.npz` null distributions + draft methodological-disclosure paragraph (defensive for academic record). Phase A in progress: read existing `.claude/hooks/` conventions + worktree-fixed `_battery_core.py` → author T1.36 contracts retroactively as worked example → lock grammar against artefact → build extraction skill + pre-commit hook. **tda-agent Worker Handoff staged 2026-05-27 12:31:** Worker (instance 1) authored handoff for future instance 2 in `.apm/bus/tda-agent/handoff.md` + Handoff Log at `.apm/memory/handoffs/tda-agent/handoff-01.log.md`. Handoff frames T1.37 resumption from clean state once merge lands. Manager processes the Worker Handoff per APM §3.1 only when instance 2 submits its first report.
- **2026-05-25 (late): Manager 2 handoff session — null-layer audit closed, T1.2 batch merged, APM vault rules corrected.** Handoff Log + code-review triage processed; P0-3 + P1-5 confirmed by direct code reads (`permutation_nulls.py:53-92` PH-invariant; `ngram_embed.py:115-241` re-fits PCA per call). User-directed minimal-fix path adopted: T1.36 (code-side frozen-loadings) + T1.37 (rerun) added to Plan/Tracker. [NEGATIVE] vault entry filed at top of `04-Methods/Computational-Log.md`; 2026-05-22 BHPS-shuffle CONVENTIONS rule reversed + Markov-1 PROVISIONAL rule added. APM vault-write rules corrected across CLAUDE.md, `.apm/spec.md`, `.apm/tracker.md`, `.claude/instructions/vault-integration.instructions.md` — `vault_observe` clarified as cross-session breadcrumb only; canonical entries via Write/Edit at top of page in reverse-chrono. Feedback memory `feedback_vault_writes.md` saved. **Worker-side coincidence:** tda-agent independently diagnosed and executed the same vault-discipline migration during the T1.2 batch closeout (11 historical entries backfilled to Computational-Log; vault-discipline [DECISION] filed at top with User approval). The Worker's locked convention is now the canonical reference; my session edits aligned to it. **T1.2b–h batch merged** to main at commit bf23f4c — 9 files / 724 insertions (6 result JSONs + 2 pre-existing T1.2a JSONs + pipeline addition `915f1cf` for length-matched script + `_battery_core.run_headline` refactor). Branch deleted; worktree directory `.apm/worktrees/run-stage1-headline-batch/` persists as on-disk orphan due to Windows file lock — needs manual `rm -rf` when lock releases. **Pre-fix caches preserved at PROJ_ROOT** (~150MB across 3 `.npz` files in `results/trajectory_tda_integration/stage1/cache/`) as baseline for T1.37 post-fix comparison.
- **2026-05-25: incoming Manager session.** Handoff Bus was empty; state reconstructed from Tracker, message bus, and git. Three pre-existing state divergences found and reconciled: (1) T1.32 merged 2026-05-16 at commit `11eb993` but Tracker still showed Active — corrected to Done; (2) panel-statistics-agent-2's sole task was T1.32, instance retired; (3) `run/sensitivity-independence` is a stale local branch (already merged at `3b0fc74`, 0 commits ahead) — awaiting User OK to delete.
- **2026-05-25 (late): CRITICAL — external code review confirms null-layer is structurally broken.** Reviewer flagged label-shuffle and cohort-shuffle nulls in `trajectory_tda/topology/permutation_nulls.py` (lines 53-92) as topologically invariant (pure row permutation of an already-computed embedding matrix — PH is row-order invariant). Manager code read on 2026-05-25 confirmed: `_label_shuffle` is `embeddings[rng.permutation(...)]`, `_cohort_shuffle` is within-cohort row permutation. **Invalidates all reported label-shuffle and cohort-shuffle p-values, including the 2026-05-22 "L=5000 BHPS clean negative controls" finding that triggered the CONVENTIONS edit.** Observed p ≈ 0.5 for these nulls is landmark-subsampling variance, not a label-shuffle test. P1-5 (null embeddings re-fit per call) partially confirmed for `_order_shuffle` / `_markov_shuffle` / `_stratified_markov_shuffle` — depends on `ngram_embed()` internals; if PCA basis is re-fit per call, Markov-1 headline results (T1.2/T1.3) are also corrupted by coordinate-frame mismatch. Full triage + investigation queue + recommendations in `.apm/memory/code-review-2026-05-25.md`. **Hold T1.33/T1.34/T1.35 dispatch.** Vault `[NEGATIVE]` filing deferred to fresh Manager session per User direction. T1.2b-h batch results arriving from `run/stage1-headline-batch` are now provisional pending null-layer audit.
- **2026-05-25: T1.21 → Spec amendment → three new Stage-1 tasks.** T1.21 Partial report surfaced structural non-estimability of the Tier 3 cross-classified GLMM (92% FOO singletons on R2/R6 subsample). User-led discussion concluded the original ten-unit triangulated FOO strategy was drifting into regression-methodology weeds at the cost of TDA-side narrative coherence; tightened to a three-task strategy centered on the topology-side FOO test. Spec amended (§"Final regression specification" + §"Endogeneity / mediation framework" replaced; new §"Topological FOO Signature" section added). Plan amended (T1.21+T1.22 STATUS UPDATE blocks; T1.33+T1.34+T1.35 added; T2.4/T2.10 dependencies updated; dependency graph extended). Four vault entries filed via vault_observe to 04-Methods/Computational-Log: [NEGATIVE] T1.21 supplemental + [DECISION] pre-regs for T1.33, T1.34c, T1.35. Formal causal mediation (Imai-Keele-Tingley) deferred to follow-up paper. T1.33/T1.34/T1.35 all Ready; dispatch awaiting User direction (and may coordinate with T1.2 overnight batch on tda-agent).
- **2026-05-14 (afternoon): panel-stats workers held pending coordinated code-review fix batch.** panel-statistics-agent-1 is idle (T1.21 dispatched to bus but not yet started; HOLD issued); panel-statistics-agent-2 is mid-batch on `run/sensitivity-independence` and will pause after current dispatch (HALT-after-batch in bus). T1.21 deferred until post-fix because it consumes T1.18 NS-SEC proxy values that B4 fixes may alter. Workers stay idle/halted until both have reported and User has reviewed the fix-batch dispatch plan.
- **2026-05-14 (afternoon): Three-batches-by-file-domain fix plan (User-confirmed structure).** Batches branch off main as separate dispatch units to keep merges clean. Detailed task scoping deferred until dispatch time. High-level batch composition: **Batch 1 (`pipe/coderabbit-manski`)** — A3 undefined `%||%` in `manski_bounds.R`; B1 WA full-pessimism allocation + portability + JSON-load safety in `manski_bounds_conditional.R`. **Batch 2 (`pipe/coderabbit-mice`)** — B2 Rubin's-rules SE/FMI in `mice_income.R`; B4(a)/B4(b) NS-SEC field rename + per-cluster heterogeneity diagnostic in `nssec_sibling_mice.R`; B4(c) L imputed/observed divergence is a User-decision point surfaced after the per-cluster diagnostic lands (may force MICE respec). **Batch 3 (`pipe/coderabbit-ipw-port`)** — B3 zero-income tercile inclusion + B5 SMD-before/after + propensity-density overlap by cohort × analytical-status in `ipw_construction.R`; cosmetic p-value display in regression JSONs; Python-script style fixes (assert→raise, max_iter centralisation, repo-marker path walks). Each batch ends with re-run + new dated JSONs + commit on its branch; Manager merges sequentially after Task Review.
- **2026-05-14 (afternoon): Working-tree contamination identified and triaged.** Four files modified locally without commit: `manski_bounds_conditional.R`, `regression_tier1.R`, `check_pidp_crosswalk.R`, `run_w2_internal_p_audit.py`. Source confirmed by User: CodeRabbit IDE-plugin "apply suggestion" clicks against the bundle being processed. Two parse errors introduced in `manski_bounds_conditional.R` (lines 16 and 82 — newline lost between substitution and following statement). Decision: discard working-tree edits via `git checkout --` (User will execute); CodeRabbit suggestions to be re-applied properly through the three batches above with full review. Committed reproducibility at HEAD is intact; only the dirty working tree was affected.- `.apm/` git-tracking is Option B: planning artefacts tracked, runtime (`bus/`, `memory/stage-NN/`, `worktrees/`) gitignored. Pre-existing tracked bus files were untracked via `git rm --cached`.
- Multi-terminal compute is User-confirmed; expect Stage 1 to dispatch parallel TDA + Panel-Statistics work in worktrees.
- Vault reads use `vault-engine` MCP tools (`vault_get`, `vault_query`, `vault_skeleton`, `vault_status`, `vault_graph`, `cross_vault`); vault writes use `Write` / `Edit` against absolute paths under `C:\Users\steph\Documents\TDA-Research\`. `vault_observe` is for cross-session breadcrumbs only — it does not append to a page.
- `.apm-archive/` is reference-only historical APM v0.5.3 state; Workers must not modify or extract from it.
- UKDA T&Cs prohibit redistribution of `data/UKDA-6614-tab/`; Stage 3 standalone repos use pointers + extraction scripts only.
- Stage-boundary holistic checks: Stage 0 → 1 (verify all four pre-registrations on file); Stage 1 → 2 (verify every outcome lock has `[DECISION]` vault entry before prose dispatch); Stage 2 → 3 (T2.22 humanizer + final notation as v2-completion gate); Stage 3 (T3.3 two-machine repo verification as milestone acceptance check).
- T0.1 Done (commit `214586e`): sklearn 1.8.0 is the confirmed pin — sklearn 1.3.x has no Python 3.13 cp313 wheels. giotto-tda removed from locked environment (no cp313 wheels); TDA pipeline uses gudhi/ripser/persim directly. Any `gtda.*` imports in pipeline code will fail under the locked env.
- numpy 2.x now in use: deprecated `np.bool`, `np.int`, `np.float` aliases raise errors; T0.2 RNG audit should flag any such usage encountered. TDA Agent Tasks 0.4–0.8 should be aware.
- BLAS env loading is manual: `uv run --env-file .env python script.py`; `[tool.uv] env-file` not supported in the installed uv version.
- T0.2 Done (commit `b8ef1cb`): 1 unseeded production-path call fixed (`markov_ladder.simulate_markov_trajectories`); 2 entry-point scripts threaded (`run_pipeline.py --seed`, `bhps_tda_pipeline.py MASTER_SEED=42`). Canary reference values: H0=1138.24331880, H1=78.97751522 (L=500, n_perms=20, seed=42) — used as reference for T0.3 two-machine comparison.
- T0.3 Blocked at second-machine coordination step (commit `1991de2` on `pipe/two-machine-check`). Local canary matches T0.2 reference exactly (H0=1138.24331880, H1=78.97751522). Awaiting `canary_machine2_2026-05-07.json` placed at `results/trajectory_tda_integration/repro/` in the worktree. T0.3 only blocks T2.18 (Stage 2 prose); Stage 1 is unaffected. Resume via `/apm-4-check-tasks` once file is placed.
- T0.4/T0.5/T0.6 batch Done (commits 15fa9a3/043f0b0/4c73a1a). Key findings: (a) Markov-2 Laplace smoothing implemented (alpha=1 default, 13 tests in tests/trajectory/). (b) T0.5 root cause confirmed: sklearn 1.3.2 pkl corrupt under 1.8.0; fix already in codebase — `load_regime_labels` reads from `05_analysis.json` (27,280 labels, 7 regimes, v1 Table 2 match); input validation guards added. (c) T0.6: `detect_eps_star_knee()` extracted; median ε*=0.54 from knee_analysis.json (LOCKED: Option A, data-driven); 4 degenerate years (2003/2005/2011/2019).
- T0.7/T0.8 batch Done (commits 2bed613/4bda7ef+335dbf9, branch `pipe/w2-fixes` merged). Key findings: (a) T0.7 W₂ ground-metric audit: canonical call locked as `gudhi.wasserstein.wasserstein_distance(dgm1, dgm2, order=2, internal_p=2)`; ℓ∞ sensitivity confirmed qualitative agreement with ℓ² (H0: ℓ∞ p=0.008, ℓ² p=0.002 — both reject; H1: ℓ∞ p=0.224, ℓ² p=0.086 — both fail to reject); DECISION vault entry filed pre-run. (b) T0.8 W₂ test construction: `compute_w2_ratio_bca_ci()` added to `vectorisation.py` using `scipy.stats.bootstrap(method='BCa')`; `t_ratio`, `bca_ci_lower`, `bca_ci_upper` now recorded in per-dimension wasserstein results; 16 unit tests pass; latent `gudhi.wasserstein` import bug fixed (was silently falling to greedy approximation via bare `import gudhi`). `pot` optional-extras gap surfaced by both tasks.
- T0.9/T0.10/T0.11 batch Done (commits ea6f657/de724f6/74e147d). Key findings: (a) jbstat locked recoding: E={1,2,5,11,12,13,14,15}, U={3,9}, I={4,6,7,8,10,97}; codes 11/13/14/15 corrected from initial guesses. (b) Canonical income variable `fihhmngrs_dv`; BHPS cohort enrolls from wave b; 10,992 spanning individuals; tercile concordance 68.9% (FLAG <80%); TERCILE BOUNDARY LOCKED: Option A (within-era). (c) FOO clusters: 27,972 components, 99.94% coverage, ICC=0.899 upper bound; CSV at `data/derived/foo_clusters_2026-05-06.csv` (gitignored, on disk).
- Stage 1 parallel dispatch completed 2026-05-13. TDA Agent: T1.1–T1.3 batch on branch `run/core-tda-battery` (worktree `.apm/worktrees/run-core-tda-battery`). Panel-Statistics Agent: T1.12–T1.14 batch on branch `run/bic-ipw-mice-income` (worktree `.apm/worktrees/run-bic-ipw-mice-income`). `.env` copied to both worktrees. Stage-01 memory directory created at `.apm/memory/stage-01/`.
- `pot` optional extras gap: `pot>=0.9.0` is under `[project.optional-dependencies] wasserstein` in pyproject.toml; `uv sync` alone does not install it. Main venv fixed with `uv sync --extra wasserstein` (pot==0.9.6.post1 installed). Worktrees require `uv pip install pot` separately. Consider moving to core deps to avoid recurrence.
- T1.12/T1.13/T1.14 Done (commits 9e6fb67/6d6fd65/4d4861c, merged 2026-05-13). Key findings: (a) BIC global minimum k=14 (ΔBIC=504,751 vs k=7, very strong); k=7 is locally optimal in k=6–8 neighbourhood; paper sections on k-selection MUST include this BIC disclosure. (b) `lwtresp` does not exist in UKDA-6614 data — corrected to `{wave}_indinub_lw` (UKHLS c+) / `{wave}_indin91_lw` (BHPS bb+); wave ba has no longitudinal weight (lw_base=1). AUC=0.714, ESS=57,035. (c) MICE FMI 2–5% throughout; strong income-regime alignment: R1=72.7% H, R2=63.9% L, R6=77.0% L (external validation of GMM regimes). T1.14 deduplication fix: first non-NA row per (pidp, wave) before dcast.
- panel-statistics-agent task log protocol deviation: T1.12/T1.13/T1.14 logs not written by agent; Manager wrote them from batch report content. Task logs verified against committed result files. Resolved — no action needed from agent.
- T1.13 vault entry type error: agent filed `[PIPELINE]` but correct type is `[RESULT]` (AUC=0.714, ESS=57,035 are citable numerical results). Plan corrected to `[RESULT]`. Vault entry already filed as `[PIPELINE]` — supplementary `[RESULT]` entry filed 2026-05-13 via vault_observe.
- T1.15/T1.18/T1.19 batch reviewed 2026-05-13. T1.18 (sibling MICE NS-SEC): Done — proxy is dominant parental class: `pasoc90_cc` (father's SOC90) falling back to `masoc90_cc` (mother's SOC90) when father missing; agent correctly implemented this (76.3% → 83.3% combined → 91.5% post-propagation); no direct parental NS-SEC in UKDA-6614 confirmed via documentation search; proxy approach confirmed acceptable by Manager. T1.19 (Tier 1 regression): rerun required — outcome was applied to all 27,280 individuals (72.6% "escape" = regime prevalence) not conditional on starting in R2/R6 (5.6% conditional escape in v1). T1.15 (Manski bounds): regime share bounds correct; conditional escape rate bounds (5.6% → pessimistic lower bound) missing — supplement required. T1.19 rerun + T1.15 supplement dispatched as follow-up batch on same branch.
- T1.13 IPW extreme weight (raw max=535.89) explained by birth cohort structure: `birth_cohort_group1990+` coefficient=−6.196 → propensity≈0.002 for recent entrants who cannot satisfy the 10-year continuity criterion by construction. This conflates structural impossibility with differential attrition in the eligible population (n=113,411). p1–p99 trimming is correct per reviewer spec (trimmed max=42.78, CV 1.36→0.99). T1.29 added as structural-eligibility sensitivity (restrict eligible population to individuals enrolled early enough to have ≥10 waves available). Vault `[RESULT]` and `[DECISION]` entries filed 2026-05-13.
- T1.15-supplement/T1.19-rerun follow-up batch reviewed 2026-05-13. Root-cause confirmed (two compounding errors in Manager's prior clarification): (1) escape was defined as last-observed jbstat ∈ E — WRONG; v1 escape is window-based (first window ∈ {R2,R6} → any subsequent window ∉ {R2,R6}), implemented in `run_priority2.py::run_age_stratified()`; (2) n=4,832 regression sample was attributed to an age_at_entry<60 filter — WRONG; it is the complete-case (NS-SEC observed) subset of 7,453 first-window R2/R6 starters. v1 source-of-record confirmed: `results/trajectory_tda_priority2/p2_5_age_stratified.json` (n_starters=7,453, n_escaped=416, escape_rate=5.59%, regression n_obs=4,832, OR(R6)=20.56). Additional complication: `05_gmm.joblib` unreadable under sklearn 1.8.0 (T0.5 known) — agent must refit GMM (k=7, full covariance, n_init=5, seed=42) before window prediction. Corrected guidance dispatched 2026-05-13; task bus fully rewritten with window-based methodology and explicit reference to `run_priority2.py`.
- T1.15-supplement/T1.19-rerun (second follow-up) reviewed 2026-05-14 (commit 5c06be2, merged to main). Key outcomes: (a) T1.15-supplement DONE — Manski conditional escape bounds: full-pessimism 0.44%, partial-pessimism 1.3% overall; (b) T1.19-rerun DONE — 5 bugs fixed by agent; critical finding: GOOD_REGIMES={1,4} not just "any ∉ {R2,R6}" — v1 escape requires reaching R1 or R4 specifically (from run_priority2.py); T1.20 must use window_escape_assignments_2026-05-14.json for the escape indicator; ESC-1 ACCEPTED (irrecoverable pkl causes 10% escape_rate boundary deviation); ESC-2 ACCEPTED Option B (n_cc=6,173 vs 4,832 — T1.18 proxy has better coverage, correct v2 improvement); GMM refit pkl at 05_gmm_refit_2026-05-14.pkl. PCA artifacts (02_scaler.joblib, 02_pca.joblib) permanently lost — all downstream tasks requiring GMM prediction must use svd_solver='full' refit approach. Pipe/w2-fixes worktree cleaned up (was merged but not removed). Two new worktrees created: run-tier2-regression (T1.20) and run-sensitivity-independence (T1.16/17/23/24/25/27/29). Second panel-statistics-agent instance (instance 2) dispatched to sensitivity worktree via bus panel-statistics-agent-2.
- T1.27 and T1.25 both require PCA re-embedding: use svd_solver='full' (deterministic LAPACK) to match T1.19 rerun approach. Original PCA pkl unrecoverable — document in each task's output. T1.27 "frozen PCA loadings" means the T1.19-refit PCA (not the original), maintaining comparability within v2.
- T1.20 Done (commit 0ae5398, merged 68f0513): σ²_u=0.208, ICC=0.060, marginal McFadden R²=0.486. regime_6 OR=21.3 dominates (vs T1 Firth OR=18.6). Conditional R²=−0.176 (artefact; paper cites marginal only). All 11 key predictors direction-consistent T1→T2. T1.21 dispatched to run/tier3-regression.
- IPW individual weights gap (2026-05-14): ipw_construction.R only saves diagnostics JSON — no individual weights RDS ever written. Fix: add saveRDS block to ipw_construction.R, re-run, commit on run/sensitivity-independence. Required before T1.16 can run. Clarification added to panel-statistics-agent-2 bus.
- H0 tree-cut labels gap (2026-05-14): H0 components at eps*=0.54 not pre-computed anywhere. T1.23 derives them from embeddings.npy via sklearn radius_neighbors_graph + connected_components (or AgglomerativeClustering linkage=single). GMM labels in 05_analysis.json[gmm_labels][27280]. Clarification in agent-2 bus.
- **2026-05-22 (afternoon): T0.12 phase-split merged to main + T2.1/T2.2 evidenced-prose merged to main + T1.2 resumed batch dispatched.** Three merges to main today: (1) `pipe/stage1-phase-split` (T0.12) — 5 phase scripts under trajectory_tda/scripts/stage1/ + permutation cache .npz schema + partial-JSON writes + launch-PID markers + smoke-test equivalence with legacy at L=200/B=10 (1e-10 tolerance) + .gitignore patch for launches/cache/.partial; (2) `paper/p01a-v2-evidenced` — T2.1 8 evidenced section files (R1-M5/M1/H3/10.1/10.3/10.5 + R3-B2 Tier 1 + R3-B5) + T2.2 §3.3 Edgington formula correction + §S0 footnote + Table 1 caveat; (3) cherry-picked from run/core-tda-battery: T1.1 pre-reg JSON + T1.3 stratified result JSON + run_stratified_battery.py producing script. T1.2 resumed batch (T1.2a–h, 8 sub-tasks) dispatched to tda-agent on new branch run/stage1-headline-batch — symmetric B=1000 throughout per User mandate; Entry 5 length-match bundled (Strategies (a) truncate + (b) first13). ~60h sequential overnight execution; uses new phase-split pipeline (halt-resilient via .partial/ writes + launch-PID markers + cache-driven landscape sensitivity).
- **2026-05-22 (afternoon): repomix-output.xml history rewrite + force-push.** Push to origin/main rejected by GitHub 100MB pre-receive hook because commit 52af787 added repomix-output.xml at 108MB. User authorized history rewrite (option a — file is regenerable derivative artefact, no archival value). git-filter-repo via uvx removed the file from all 323 commits across all refs in 3.39s. Backup tag pre-filter-repo-backup retained at pre-rewrite main HEAD. Force-pushed main with --force-with-lease (after fetch to refresh stale origin/main ref following origin re-add). Working trees on three branches showed stale index entries staging the file (`A repomix-output.xml`); resolved via `git reset HEAD` then physical file delete (~336MB disk freed across 3 worktrees). All 6 worktree branches rewritten consistently by filter-repo. Side effect: every worktree branch SHA changed (paper/p01a-v2-evidenced dc1d2a1→a88cfa6, pipe/stage1-phase-split f905965→cbc5d7e, pipe/two-machine-check 1991de2→57684e3, run/core-tda-battery 275c16b→65bf477, run/tier3-regression e4f40cb→6cc390b). repomix-output.xml + repomix-output.* added to .gitignore to prevent re-introduction. Final main HEAD on origin after both Stage 0 and Stage 2 merges: 0a91d94.
- **2026-05-22 (afternoon): vault [DECISION] filed — BHPS L=5000 clean negative controls.** L=5000 BHPS post-audit shows label-shuffle H₀ p=0.512/H₁ p=0.558, cohort-shuffle H₀ p=0.554/H₁ p=0.634 — all clean. L=2000 finding (label p=0.036, cohort p=0.034) that founded the CONVENTIONS "NEVER treat BHPS shuffles as assumed negative controls" rule was a landmark-budget artefact. Order-shuffle and Markov-1 rejections at L=5000 BHPS unchanged (both p=0.000) — headline finding intact. CONVENTIONS.md rule manually replaced by User following the Manager's suggested replacement text. P01-A §4.3 worker section already reflects the new finding (L=5000 clean + L=2000 outlier preserved in Supplement §S4 landmark-budget sensitivity).
- **2026-05-24 (later still): pre-reg amendments filed + T1.2b–h resume dispatched + T2.3 §3 prose dispatched.** Pre-reg #1 amendment (LM sensitivity at L ∈ {2500, 8000} now confirmatory scope, not exploratory) and pre-reg #5 redo (BHPS length-matched, Outcome A/B/C labels replacing the polarity-ambiguous prior phrasing) filed in the vault via `vault_observe` linked to `04-Methods/Computational-Log`. T1.2b–h dispatched on existing `run/stage1-headline-batch` worktree (Worker must pull main first to pick up the T0.13 phase-script fix); ~60h sequential overnight expected; pre-flight discipline mandated per the F4 T1.2a self-noted rule. **T2.3 dispatched** to academic-writing-agent on new `paper/p01a-v2-section3-intrinsic-d` worktree (`.env` copied) — uses tda-agent's pre-drafted longer-variant prose from the T1.4 report (the variant that pre-empts the USoc landmark-d ≈ 10 question for any methodologist reviewer); per-section User review mandated before marking complete. Both agents now active in parallel with the still-running T1.21 panel-statistics regression on `run/tier3-regression`.
- **2026-05-24 (later): T0.13 + T1.4 both Done + merged.** T0.13 (reproducibility-agent, ~25min walltime) merged at b66ca02 — three Stage-1 phase scripts default to absolute PROJ_ROOT-anchored paths via `core.proj_root()`; `_battery_core.proj_root()` itself unchanged (already correct). Worktree-CWD `--help` verification clean. **T1.4 (tda-agent, 37.6s walltime) merged at 1776720** — both USoc and BHPS 20-D embeddings test OVER-DIMENSIONAL per the decision rule (TwoNN full-support 3.59 USoc / 3.77 BHPS, both ≤ 10; TwoNN-vs-MLE gap 2.21/2.53, well within consistency tolerance). PCA-20 at 49% explained variance over-captures the manifold; the discarded 51% is largely noise. **No T1.5 D-sweep recommended.** Damrich/Hiraoka curse-of-dim concern empirically minimal for the P01-A pipeline. Worker provided ready-to-paste §3 methods prose (short + longer variants) for next academic-writing-agent dispatch. Sub-findings: ~40% USoc / ~33% BHPS duplicate trajectories in 20-D (real property, handled correctly by W₂ test); USoc landmark d=10.06 *just over* the OVER/WELL-ALIGNED boundary (longer prose variant addresses); skdim.id.MLE broken on Py3.13 (Worker implemented Levina-Bickel inline); stale .COMMIT_MSG scaffold in worktree noted; jcodemunch tooling-gap concern (F5 from T1.2a + #5 here) now MOOT — retired in cleanup commit 6ff37f8. Two worktrees removed; branches deleted. Five stale bus reports cleared (T2.2 + T1.30 + T1.32 already merged 2026-05-15..05-22).
- **2026-05-24: T1.2a Done; T0.13 + T1.4 dispatched in parallel.** T1.2a USoc headline (Markov-1, L=5000, B=1000, n_null_pairs=500) completed in 2h58 on `run/stage1-headline-batch` (commit cb5eb3e). Result: H₀ W₂ p=0.0020 (floor)/Land L² p=0.0818 — **informative metric divergence** validating the T0.7 dual-metric lock; H₁ W₂ p=0.0020/Land L² p=0.0160 — both reject. Worker stopped batch early after diagnosing relative-default `--usoc-dir`/`--bhps-dir` defect (FileNotFoundError under worktree CWD). Manager response: (a) approved T1.2a with substantive note on H₀ metric divergence to be addressed in P01-A §4 prose once T1.2b lands; (b) dispatched **T0.13** (reproducibility-agent, `pipe/phase-script-projroot-defaults`, ~30-60min) to fix the three Stage-1 phase scripts' defaults via `core.proj_root()`; (c) dispatched **T1.4** (tda-agent, `run/intrinsic-dimensionality-estimate`, ~1h) to estimate intrinsic-d via TwoNN + MLE on the 20-D PCA-projected USoc/BHPS embeddings (raw=90D bigram counts, PCA-cut to D=20 capturing 48.96%/49.02% of variance). T1.4 directly addresses Damrich-Berens-Kobak (2024 arXiv:2311.03087) curse-of-dim concern for VR-PH on noisy high-D Euclidean point clouds + Hiraoka et al. (2024) PCA-preprocessing critique; diagnostic decision rule embedded (≤10 over-dim safe / 10–18 borderline / >18 triggers D-sweep T1.5). Both new worktrees `.env`-copied. Two pre-reg housekeeping items also outstanding for User: pre-reg #1 amendment (LM sensitivity L∈{2500,8000} confirmatory scope) and pre-reg #5 redo from scratch (BHPS length-match polarity; new Outcome A/B/C labels eliminate prior ambiguity).
